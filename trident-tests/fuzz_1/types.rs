//! # Trident Generated Types
//!
//! This file is automatically generated by Trident.
//! **DO NOT EDIT THIS FILE MANUALLY**

#![allow(dead_code)]
#![allow(unused_imports)]

use borsh::BorshDeserialize;
use borsh::BorshSerialize;
use trident_fuzz::fuzzing::*;

// ============================================================================
// PROGRAM MODULES
// ============================================================================

// ----------------------------------------------------------------------------
// Program: soladrome
// ----------------------------------------------------------------------------
pub mod soladrome {
    use super::*;

    // ------------------------------------------------------------------------
    // Program ID
    // ------------------------------------------------------------------------

    /// Returns the program ID for soladrome
    pub fn program_id() -> Pubkey {
        pubkey!("4d2SYx8Dzv5A4X5FcHtvNhTFM582DFcioapnaSUQnLQd")
    }

    // ------------------------------------------------------------------------
    // Instructions
    // ------------------------------------------------------------------------

    // ....................................................................
    // Instruction: AddLiquidity
    // ....................................................................

    /// Main instruction struct for AddLiquidity
    pub struct AddLiquidityInstruction {
        pub accounts: AddLiquidityInstructionAccountMetas,
        pub data: AddLiquidityInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AddLiquidity instruction
    #[derive(Debug, Clone, Default)]
    pub struct AddLiquidityInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub token_a_vault: AccountMeta,

        pub token_b_vault: AccountMeta,

        pub user_token_a: AccountMeta,

        pub user_token_b: AccountMeta,

        pub user_lp: AccountMeta,

        pub lp_dead_ata: AccountMeta,

        pub lp_dead: AccountMeta,

        pub lp_user_info: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub user_o_sola: AccountMeta,

        pub rent: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for AddLiquidity instruction
    #[derive(Debug, Clone)]
    pub struct AddLiquidityInstructionAccounts {
        pub user: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub token_a_vault: Pubkey,

        pub token_b_vault: Pubkey,

        pub user_token_a: Pubkey,

        pub user_token_b: Pubkey,

        pub user_lp: Pubkey,

        pub lp_dead_ata: Pubkey,

        pub lp_user_info: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub user_o_sola: Pubkey,
    }

    impl AddLiquidityInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            token_a_vault: Pubkey,

            token_b_vault: Pubkey,

            user_token_a: Pubkey,

            user_token_b: Pubkey,

            user_lp: Pubkey,

            lp_dead_ata: Pubkey,

            lp_user_info: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            user_o_sola: Pubkey,
        ) -> Self {
            Self {
                user,

                pool,

                lp_mint,

                token_a_vault,

                token_b_vault,

                user_token_a,

                user_token_b,

                user_lp,

                lp_dead_ata,

                lp_user_info,

                protocol_state,

                o_sola_mint,

                user_o_sola,
            }
        }
    }

    /// Instruction data for AddLiquidity
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AddLiquidityInstructionData {
        pub amount_a_desired: u64,

        pub amount_b_desired: u64,

        pub min_lp: u64,
    }

    impl AddLiquidityInstructionData {
        pub fn new(amount_a_desired: u64, amount_b_desired: u64, min_lp: u64) -> Self {
            Self {
                amount_a_desired,

                amount_b_desired,

                min_lp,
            }
        }
    }

    /// Implementation for AddLiquidityInstruction
    impl AddLiquidityInstruction {
        fn discriminator() -> [u8; 8] {
            [181u8, 157u8, 89u8, 67u8, 143u8, 182u8, 52u8, 72u8]
        }

        pub fn data(data: AddLiquidityInstructionData) -> Self {
            Self {
                accounts: AddLiquidityInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AddLiquidityInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new(accounts.lp_mint, false);

            self.accounts.token_a_vault = AccountMeta::new(accounts.token_a_vault, false);

            self.accounts.token_b_vault = AccountMeta::new(accounts.token_b_vault, false);

            self.accounts.user_token_a = AccountMeta::new(accounts.user_token_a, false);

            self.accounts.user_token_b = AccountMeta::new(accounts.user_token_b, false);

            self.accounts.user_lp = AccountMeta::new(accounts.user_lp, false);

            self.accounts.lp_dead_ata = AccountMeta::new(accounts.lp_dead_ata, false);

            self.accounts.lp_dead =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.lp_user_info = AccountMeta::new(accounts.lp_user_info, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.user_o_sola = AccountMeta::new(accounts.user_o_sola, false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.token_a_vault.clone());

            metas.push(self.accounts.token_b_vault.clone());

            metas.push(self.accounts.user_token_a.clone());

            metas.push(self.accounts.user_token_b.clone());

            metas.push(self.accounts.user_lp.clone());

            metas.push(self.accounts.lp_dead_ata.clone());

            metas.push(self.accounts.lp_dead.clone());

            metas.push(self.accounts.lp_user_info.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.user_o_sola.clone());

            metas.push(self.accounts.rent.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: AmmSwap
    // ....................................................................

    /// Main instruction struct for AmmSwap
    pub struct AmmSwapInstruction {
        pub accounts: AmmSwapInstructionAccountMetas,
        pub data: AmmSwapInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for AmmSwap instruction
    #[derive(Debug, Clone, Default)]
    pub struct AmmSwapInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool: AccountMeta,

        pub token_a_vault: AccountMeta,

        pub token_b_vault: AccountMeta,

        pub user_token_in: AccountMeta,

        pub user_token_out: AccountMeta,

        pub market_vault: AccountMeta,

        pub protocol_state: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for AmmSwap instruction
    #[derive(Debug, Clone)]
    pub struct AmmSwapInstructionAccounts {
        pub user: Pubkey,

        pub pool: Pubkey,

        pub token_a_vault: Pubkey,

        pub token_b_vault: Pubkey,

        pub user_token_in: Pubkey,

        pub user_token_out: Pubkey,

        pub market_vault: Pubkey,

        pub protocol_state: Pubkey,
    }

    impl AmmSwapInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool: Pubkey,

            token_a_vault: Pubkey,

            token_b_vault: Pubkey,

            user_token_in: Pubkey,

            user_token_out: Pubkey,

            market_vault: Pubkey,

            protocol_state: Pubkey,
        ) -> Self {
            Self {
                user,

                pool,

                token_a_vault,

                token_b_vault,

                user_token_in,

                user_token_out,

                market_vault,

                protocol_state,
            }
        }
    }

    /// Instruction data for AmmSwap
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct AmmSwapInstructionData {
        pub amount_in: u64,

        pub min_out: u64,

        pub a_to_b: bool,
    }

    impl AmmSwapInstructionData {
        pub fn new(amount_in: u64, min_out: u64, a_to_b: bool) -> Self {
            Self {
                amount_in,

                min_out,

                a_to_b,
            }
        }
    }

    /// Implementation for AmmSwapInstruction
    impl AmmSwapInstruction {
        fn discriminator() -> [u8; 8] {
            [129u8, 102u8, 244u8, 12u8, 83u8, 130u8, 166u8, 173u8]
        }

        pub fn data(data: AmmSwapInstructionData) -> Self {
            Self {
                accounts: AmmSwapInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: AmmSwapInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.token_a_vault = AccountMeta::new(accounts.token_a_vault, false);

            self.accounts.token_b_vault = AccountMeta::new(accounts.token_b_vault, false);

            self.accounts.user_token_in = AccountMeta::new(accounts.user_token_in, false);

            self.accounts.user_token_out = AccountMeta::new(accounts.user_token_out, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.token_a_vault.clone());

            metas.push(self.accounts.token_b_vault.clone());

            metas.push(self.accounts.user_token_in.clone());

            metas.push(self.accounts.user_token_out.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: BorrowUsdc
    // ....................................................................

    /// Main instruction struct for BorrowUsdc
    pub struct BorrowUsdcInstruction {
        pub accounts: BorrowUsdcInstructionAccountMetas,
        pub data: BorrowUsdcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for BorrowUsdc instruction
    #[derive(Debug, Clone, Default)]
    pub struct BorrowUsdcInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub user_usdc: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for BorrowUsdc instruction
    #[derive(Debug, Clone)]
    pub struct BorrowUsdcInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,

        pub user_usdc: Pubkey,

        pub user_position: Pubkey,
    }

    impl BorrowUsdcInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,

            user_usdc: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                hi_sola_mint,

                user_hi_sola,

                floor_vault,

                market_vault,

                user_usdc,

                user_position,
            }
        }
    }

    /// Instruction data for BorrowUsdc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct BorrowUsdcInstructionData {
        pub usdc_amount: u64,
    }

    impl BorrowUsdcInstructionData {
        pub fn new(usdc_amount: u64) -> Self {
            Self { usdc_amount }
        }
    }

    /// Implementation for BorrowUsdcInstruction
    impl BorrowUsdcInstruction {
        fn discriminator() -> [u8; 8] {
            [31u8, 120u8, 217u8, 32u8, 182u8, 137u8, 251u8, 34u8]
        }

        pub fn data(data: BorrowUsdcInstructionData) -> Self {
            Self {
                accounts: BorrowUsdcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: BorrowUsdcInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new_readonly(accounts.user_hi_sola, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: BuySola
    // ....................................................................

    /// Main instruction struct for BuySola
    pub struct BuySolaInstruction {
        pub accounts: BuySolaInstructionAccountMetas,
        pub data: BuySolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for BuySola instruction
    #[derive(Debug, Clone, Default)]
    pub struct BuySolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub user_usdc: AccountMeta,

        pub user_sola: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for BuySola instruction
    #[derive(Debug, Clone)]
    pub struct BuySolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub user_usdc: Pubkey,

        pub user_sola: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,
    }

    impl BuySolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            user_usdc: Pubkey,

            user_sola: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                sola_mint,

                user_usdc,

                user_sola,

                floor_vault,

                market_vault,
            }
        }
    }

    /// Instruction data for BuySola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct BuySolaInstructionData {
        pub usdc_in: u64,

        pub min_sola_out: u64,
    }

    impl BuySolaInstructionData {
        pub fn new(usdc_in: u64, min_sola_out: u64) -> Self {
            Self {
                usdc_in,

                min_sola_out,
            }
        }
    }

    /// Implementation for BuySolaInstruction
    impl BuySolaInstruction {
        fn discriminator() -> [u8; 8] {
            [107u8, 36u8, 174u8, 143u8, 33u8, 193u8, 24u8, 35u8]
        }

        pub fn data(data: BuySolaInstructionData) -> Self {
            Self {
                accounts: BuySolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: BuySolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.user_sola = AccountMeta::new(accounts.user_sola, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.user_sola.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CheckpointLp
    // ....................................................................

    /// Main instruction struct for CheckpointLp
    pub struct CheckpointLpInstruction {
        pub accounts: CheckpointLpInstructionAccountMetas,
        pub data: CheckpointLpInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CheckpointLp instruction
    #[derive(Debug, Clone, Default)]
    pub struct CheckpointLpInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub user_lp: AccountMeta,

        pub lp_user_checkpoint: AccountMeta,

        pub pool_epoch_accum: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for CheckpointLp instruction
    #[derive(Debug, Clone)]
    pub struct CheckpointLpInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub user_lp: Pubkey,

        pub lp_user_checkpoint: Pubkey,

        pub pool_epoch_accum: Pubkey,
    }

    impl CheckpointLpInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            user_lp: Pubkey,

            lp_user_checkpoint: Pubkey,

            pool_epoch_accum: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                pool,

                lp_mint,

                user_lp,

                lp_user_checkpoint,

                pool_epoch_accum,
            }
        }
    }

    /// Instruction data for CheckpointLp
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CheckpointLpInstructionData {
        pub epoch: u64,
    }

    impl CheckpointLpInstructionData {
        pub fn new(epoch: u64) -> Self {
            Self { epoch }
        }
    }

    /// Implementation for CheckpointLpInstruction
    impl CheckpointLpInstruction {
        fn discriminator() -> [u8; 8] {
            [3u8, 46u8, 34u8, 113u8, 116u8, 46u8, 123u8, 219u8]
        }

        pub fn data(data: CheckpointLpInstructionData) -> Self {
            Self {
                accounts: CheckpointLpInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CheckpointLpInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.pool = AccountMeta::new_readonly(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new_readonly(accounts.lp_mint, false);

            self.accounts.user_lp = AccountMeta::new_readonly(accounts.user_lp, false);

            self.accounts.lp_user_checkpoint = AccountMeta::new(accounts.lp_user_checkpoint, false);

            self.accounts.pool_epoch_accum = AccountMeta::new(accounts.pool_epoch_accum, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.user_lp.clone());

            metas.push(self.accounts.lp_user_checkpoint.clone());

            metas.push(self.accounts.pool_epoch_accum.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimBribe
    // ....................................................................

    /// Main instruction struct for ClaimBribe
    pub struct ClaimBribeInstruction {
        pub accounts: ClaimBribeInstructionAccountMetas,
        pub data: ClaimBribeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimBribe instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimBribeInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool_id: AccountMeta,

        pub reward_mint: AccountMeta,

        pub bribe_vault: AccountMeta,

        pub bribe_token_vault: AccountMeta,

        pub user_reward_ata: AccountMeta,

        pub gauge_state: AccountMeta,

        pub user_vote_receipt: AccountMeta,

        pub user_bribe_claim: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for ClaimBribe instruction
    #[derive(Debug, Clone)]
    pub struct ClaimBribeInstructionAccounts {
        pub user: Pubkey,

        pub pool_id: Pubkey,

        pub reward_mint: Pubkey,

        pub bribe_vault: Pubkey,

        pub bribe_token_vault: Pubkey,

        pub user_reward_ata: Pubkey,

        pub gauge_state: Pubkey,

        pub user_vote_receipt: Pubkey,

        pub user_bribe_claim: Pubkey,
    }

    impl ClaimBribeInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool_id: Pubkey,

            reward_mint: Pubkey,

            bribe_vault: Pubkey,

            bribe_token_vault: Pubkey,

            user_reward_ata: Pubkey,

            gauge_state: Pubkey,

            user_vote_receipt: Pubkey,

            user_bribe_claim: Pubkey,
        ) -> Self {
            Self {
                user,

                pool_id,

                reward_mint,

                bribe_vault,

                bribe_token_vault,

                user_reward_ata,

                gauge_state,

                user_vote_receipt,

                user_bribe_claim,
            }
        }
    }

    /// Instruction data for ClaimBribe
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimBribeInstructionData {
        pub epoch: u64,
    }

    impl ClaimBribeInstructionData {
        pub fn new(epoch: u64) -> Self {
            Self { epoch }
        }
    }

    /// Implementation for ClaimBribeInstruction
    impl ClaimBribeInstruction {
        fn discriminator() -> [u8; 8] {
            [215u8, 71u8, 221u8, 74u8, 40u8, 95u8, 159u8, 200u8]
        }

        pub fn data(data: ClaimBribeInstructionData) -> Self {
            Self {
                accounts: ClaimBribeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimBribeInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool_id = AccountMeta::new_readonly(accounts.pool_id, false);

            self.accounts.reward_mint = AccountMeta::new_readonly(accounts.reward_mint, false);

            self.accounts.bribe_vault = AccountMeta::new_readonly(accounts.bribe_vault, false);

            self.accounts.bribe_token_vault = AccountMeta::new(accounts.bribe_token_vault, false);

            self.accounts.user_reward_ata = AccountMeta::new(accounts.user_reward_ata, false);

            self.accounts.gauge_state = AccountMeta::new_readonly(accounts.gauge_state, false);

            self.accounts.user_vote_receipt =
                AccountMeta::new_readonly(accounts.user_vote_receipt, false);

            self.accounts.user_bribe_claim = AccountMeta::new(accounts.user_bribe_claim, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool_id.clone());

            metas.push(self.accounts.reward_mint.clone());

            metas.push(self.accounts.bribe_vault.clone());

            metas.push(self.accounts.bribe_token_vault.clone());

            metas.push(self.accounts.user_reward_ata.clone());

            metas.push(self.accounts.gauge_state.clone());

            metas.push(self.accounts.user_vote_receipt.clone());

            metas.push(self.accounts.user_bribe_claim.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimContributorHiSola
    // ....................................................................

    /// Main instruction struct for ClaimContributorHiSola
    pub struct ClaimContributorHiSolaInstruction {
        pub accounts: ClaimContributorHiSolaInstructionAccountMetas,
        pub data: ClaimContributorHiSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimContributorHiSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimContributorHiSolaInstructionAccountMetas {
        pub contributor: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub sola_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub contributor_hi_sola: AccountMeta,

        pub contributor_position: AccountMeta,

        pub contributor_vesting: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ClaimContributorHiSola instruction
    #[derive(Debug, Clone)]
    pub struct ClaimContributorHiSolaInstructionAccounts {
        pub contributor: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub sola_vault: Pubkey,

        pub market_vault: Pubkey,

        pub contributor_hi_sola: Pubkey,

        pub contributor_position: Pubkey,

        pub contributor_vesting: Pubkey,
    }

    impl ClaimContributorHiSolaInstructionAccounts {
        pub fn new(
            contributor: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            hi_sola_mint: Pubkey,

            sola_vault: Pubkey,

            market_vault: Pubkey,

            contributor_hi_sola: Pubkey,

            contributor_position: Pubkey,

            contributor_vesting: Pubkey,
        ) -> Self {
            Self {
                contributor,

                protocol_state,

                sola_mint,

                hi_sola_mint,

                sola_vault,

                market_vault,

                contributor_hi_sola,

                contributor_position,

                contributor_vesting,
            }
        }
    }

    /// Instruction data for ClaimContributorHiSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimContributorHiSolaInstructionData {}

    impl ClaimContributorHiSolaInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimContributorHiSolaInstruction
    impl ClaimContributorHiSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [94u8, 88u8, 204u8, 94u8, 187u8, 27u8, 43u8, 228u8]
        }

        pub fn data(data: ClaimContributorHiSolaInstructionData) -> Self {
            Self {
                accounts: ClaimContributorHiSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimContributorHiSolaInstructionAccounts) -> Self {
            self.accounts.contributor = AccountMeta::new(accounts.contributor, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.hi_sola_mint = AccountMeta::new(accounts.hi_sola_mint, false);

            self.accounts.sola_vault = AccountMeta::new(accounts.sola_vault, false);

            self.accounts.market_vault = AccountMeta::new_readonly(accounts.market_vault, false);

            self.accounts.contributor_hi_sola =
                AccountMeta::new(accounts.contributor_hi_sola, false);

            self.accounts.contributor_position =
                AccountMeta::new(accounts.contributor_position, false);

            self.accounts.contributor_vesting =
                AccountMeta::new(accounts.contributor_vesting, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.contributor.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.sola_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.contributor_hi_sola.clone());

            metas.push(self.accounts.contributor_position.clone());

            metas.push(self.accounts.contributor_vesting.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimContributorVesting
    // ....................................................................

    /// Main instruction struct for ClaimContributorVesting
    pub struct ClaimContributorVestingInstruction {
        pub accounts: ClaimContributorVestingInstructionAccountMetas,
        pub data: ClaimContributorVestingInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimContributorVesting instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimContributorVestingInstructionAccountMetas {
        pub contributor: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub contributor_vesting: AccountMeta,

        pub contributor_o_sola: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ClaimContributorVesting instruction
    #[derive(Debug, Clone)]
    pub struct ClaimContributorVestingInstructionAccounts {
        pub contributor: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub contributor_vesting: Pubkey,

        pub contributor_o_sola: Pubkey,
    }

    impl ClaimContributorVestingInstructionAccounts {
        pub fn new(
            contributor: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            contributor_vesting: Pubkey,

            contributor_o_sola: Pubkey,
        ) -> Self {
            Self {
                contributor,

                protocol_state,

                o_sola_mint,

                contributor_vesting,

                contributor_o_sola,
            }
        }
    }

    /// Instruction data for ClaimContributorVesting
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimContributorVestingInstructionData {}

    impl ClaimContributorVestingInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimContributorVestingInstruction
    impl ClaimContributorVestingInstruction {
        fn discriminator() -> [u8; 8] {
            [151u8, 164u8, 230u8, 161u8, 49u8, 58u8, 231u8, 44u8]
        }

        pub fn data(data: ClaimContributorVestingInstructionData) -> Self {
            Self {
                accounts: ClaimContributorVestingInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimContributorVestingInstructionAccounts) -> Self {
            self.accounts.contributor = AccountMeta::new(accounts.contributor, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.contributor_vesting =
                AccountMeta::new(accounts.contributor_vesting, false);

            self.accounts.contributor_o_sola = AccountMeta::new(accounts.contributor_o_sola, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.contributor.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.contributor_vesting.clone());

            metas.push(self.accounts.contributor_o_sola.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimFees
    // ....................................................................

    /// Main instruction struct for ClaimFees
    pub struct ClaimFeesInstruction {
        pub accounts: ClaimFeesInstructionAccountMetas,
        pub data: ClaimFeesInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimFees instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimFeesInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub market_vault: AccountMeta,

        pub user_usdc: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for ClaimFees instruction
    #[derive(Debug, Clone)]
    pub struct ClaimFeesInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub market_vault: Pubkey,

        pub user_usdc: Pubkey,

        pub user_position: Pubkey,
    }

    impl ClaimFeesInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            market_vault: Pubkey,

            user_usdc: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                hi_sola_mint,

                user_hi_sola,

                market_vault,

                user_usdc,

                user_position,
            }
        }
    }

    /// Instruction data for ClaimFees
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimFeesInstructionData {}

    impl ClaimFeesInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimFeesInstruction
    impl ClaimFeesInstruction {
        fn discriminator() -> [u8; 8] {
            [82u8, 251u8, 233u8, 156u8, 12u8, 52u8, 184u8, 202u8]
        }

        pub fn data(data: ClaimFeesInstructionData) -> Self {
            Self {
                accounts: ClaimFeesInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimFeesInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new_readonly(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new_readonly(accounts.user_hi_sola, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimFounderHiSola
    // ....................................................................

    /// Main instruction struct for ClaimFounderHiSola
    pub struct ClaimFounderHiSolaInstruction {
        pub accounts: ClaimFounderHiSolaInstructionAccountMetas,
        pub data: ClaimFounderHiSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimFounderHiSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimFounderHiSolaInstructionAccountMetas {
        pub founder: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub sola_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub founder_hi_sola: AccountMeta,

        pub founder_position: AccountMeta,

        pub founder_hi_vesting: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ClaimFounderHiSola instruction
    #[derive(Debug, Clone)]
    pub struct ClaimFounderHiSolaInstructionAccounts {
        pub founder: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub sola_vault: Pubkey,

        pub market_vault: Pubkey,

        pub founder_hi_sola: Pubkey,

        pub founder_position: Pubkey,

        pub founder_hi_vesting: Pubkey,
    }

    impl ClaimFounderHiSolaInstructionAccounts {
        pub fn new(
            founder: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            hi_sola_mint: Pubkey,

            sola_vault: Pubkey,

            market_vault: Pubkey,

            founder_hi_sola: Pubkey,

            founder_position: Pubkey,

            founder_hi_vesting: Pubkey,
        ) -> Self {
            Self {
                founder,

                protocol_state,

                sola_mint,

                hi_sola_mint,

                sola_vault,

                market_vault,

                founder_hi_sola,

                founder_position,

                founder_hi_vesting,
            }
        }
    }

    /// Instruction data for ClaimFounderHiSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimFounderHiSolaInstructionData {}

    impl ClaimFounderHiSolaInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimFounderHiSolaInstruction
    impl ClaimFounderHiSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [19u8, 134u8, 190u8, 17u8, 205u8, 232u8, 28u8, 2u8]
        }

        pub fn data(data: ClaimFounderHiSolaInstructionData) -> Self {
            Self {
                accounts: ClaimFounderHiSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimFounderHiSolaInstructionAccounts) -> Self {
            self.accounts.founder = AccountMeta::new(accounts.founder, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.hi_sola_mint = AccountMeta::new(accounts.hi_sola_mint, false);

            self.accounts.sola_vault = AccountMeta::new(accounts.sola_vault, false);

            self.accounts.market_vault = AccountMeta::new_readonly(accounts.market_vault, false);

            self.accounts.founder_hi_sola = AccountMeta::new(accounts.founder_hi_sola, false);

            self.accounts.founder_position = AccountMeta::new(accounts.founder_position, false);

            self.accounts.founder_hi_vesting = AccountMeta::new(accounts.founder_hi_vesting, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.founder.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.sola_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.founder_hi_sola.clone());

            metas.push(self.accounts.founder_position.clone());

            metas.push(self.accounts.founder_hi_vesting.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimFounderVesting
    // ....................................................................

    /// Main instruction struct for ClaimFounderVesting
    pub struct ClaimFounderVestingInstruction {
        pub accounts: ClaimFounderVestingInstructionAccountMetas,
        pub data: ClaimFounderVestingInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimFounderVesting instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimFounderVestingInstructionAccountMetas {
        pub founder: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub founder_vesting: AccountMeta,

        pub founder_o_sola: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ClaimFounderVesting instruction
    #[derive(Debug, Clone)]
    pub struct ClaimFounderVestingInstructionAccounts {
        pub founder: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub founder_vesting: Pubkey,

        pub founder_o_sola: Pubkey,
    }

    impl ClaimFounderVestingInstructionAccounts {
        pub fn new(
            founder: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            founder_vesting: Pubkey,

            founder_o_sola: Pubkey,
        ) -> Self {
            Self {
                founder,

                protocol_state,

                o_sola_mint,

                founder_vesting,

                founder_o_sola,
            }
        }
    }

    /// Instruction data for ClaimFounderVesting
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimFounderVestingInstructionData {}

    impl ClaimFounderVestingInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimFounderVestingInstruction
    impl ClaimFounderVestingInstruction {
        fn discriminator() -> [u8; 8] {
            [29u8, 26u8, 23u8, 26u8, 121u8, 145u8, 234u8, 127u8]
        }

        pub fn data(data: ClaimFounderVestingInstructionData) -> Self {
            Self {
                accounts: ClaimFounderVestingInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimFounderVestingInstructionAccounts) -> Self {
            self.accounts.founder = AccountMeta::new(accounts.founder, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.founder_vesting = AccountMeta::new(accounts.founder_vesting, false);

            self.accounts.founder_o_sola = AccountMeta::new(accounts.founder_o_sola, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.founder.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.founder_vesting.clone());

            metas.push(self.accounts.founder_o_sola.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimLpEmissions
    // ....................................................................

    /// Main instruction struct for ClaimLpEmissions
    pub struct ClaimLpEmissionsInstruction {
        pub accounts: ClaimLpEmissionsInstructionAccountMetas,
        pub data: ClaimLpEmissionsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimLpEmissions instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimLpEmissionsInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub user_o_sola: AccountMeta,

        pub pool_epoch_accum: AccountMeta,

        pub lp_user_checkpoint: AccountMeta,

        pub lp_epoch_claim: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for ClaimLpEmissions instruction
    #[derive(Debug, Clone)]
    pub struct ClaimLpEmissionsInstructionAccounts {
        pub user: Pubkey,

        pub pool: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub user_o_sola: Pubkey,

        pub pool_epoch_accum: Pubkey,

        pub lp_user_checkpoint: Pubkey,

        pub lp_epoch_claim: Pubkey,
    }

    impl ClaimLpEmissionsInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            user_o_sola: Pubkey,

            pool_epoch_accum: Pubkey,

            lp_user_checkpoint: Pubkey,

            lp_epoch_claim: Pubkey,
        ) -> Self {
            Self {
                user,

                pool,

                protocol_state,

                o_sola_mint,

                user_o_sola,

                pool_epoch_accum,

                lp_user_checkpoint,

                lp_epoch_claim,
            }
        }
    }

    /// Instruction data for ClaimLpEmissions
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimLpEmissionsInstructionData {
        pub _epoch: u64,
    }

    impl ClaimLpEmissionsInstructionData {
        pub fn new(_epoch: u64) -> Self {
            Self { _epoch }
        }
    }

    /// Implementation for ClaimLpEmissionsInstruction
    impl ClaimLpEmissionsInstruction {
        fn discriminator() -> [u8; 8] {
            [211u8, 134u8, 16u8, 226u8, 171u8, 120u8, 119u8, 230u8]
        }

        pub fn data(data: ClaimLpEmissionsInstructionData) -> Self {
            Self {
                accounts: ClaimLpEmissionsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimLpEmissionsInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool = AccountMeta::new_readonly(accounts.pool, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.user_o_sola = AccountMeta::new(accounts.user_o_sola, false);

            self.accounts.pool_epoch_accum =
                AccountMeta::new_readonly(accounts.pool_epoch_accum, false);

            self.accounts.lp_user_checkpoint = AccountMeta::new(accounts.lp_user_checkpoint, false);

            self.accounts.lp_epoch_claim = AccountMeta::new(accounts.lp_epoch_claim, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.user_o_sola.clone());

            metas.push(self.accounts.pool_epoch_accum.clone());

            metas.push(self.accounts.lp_user_checkpoint.clone());

            metas.push(self.accounts.lp_epoch_claim.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ClaimLpRewards
    // ....................................................................

    /// Main instruction struct for ClaimLpRewards
    pub struct ClaimLpRewardsInstruction {
        pub accounts: ClaimLpRewardsInstructionAccountMetas,
        pub data: ClaimLpRewardsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ClaimLpRewards instruction
    #[derive(Debug, Clone, Default)]
    pub struct ClaimLpRewardsInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub user_lp: AccountMeta,

        pub lp_user_info: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub user_o_sola: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for ClaimLpRewards instruction
    #[derive(Debug, Clone)]
    pub struct ClaimLpRewardsInstructionAccounts {
        pub user: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub user_lp: Pubkey,

        pub lp_user_info: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub user_o_sola: Pubkey,
    }

    impl ClaimLpRewardsInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            user_lp: Pubkey,

            lp_user_info: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            user_o_sola: Pubkey,
        ) -> Self {
            Self {
                user,

                pool,

                lp_mint,

                user_lp,

                lp_user_info,

                protocol_state,

                o_sola_mint,

                user_o_sola,
            }
        }
    }

    /// Instruction data for ClaimLpRewards
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ClaimLpRewardsInstructionData {}

    impl ClaimLpRewardsInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for ClaimLpRewardsInstruction
    impl ClaimLpRewardsInstruction {
        fn discriminator() -> [u8; 8] {
            [130u8, 185u8, 69u8, 158u8, 54u8, 47u8, 70u8, 79u8]
        }

        pub fn data(data: ClaimLpRewardsInstructionData) -> Self {
            Self {
                accounts: ClaimLpRewardsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ClaimLpRewardsInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new_readonly(accounts.lp_mint, false);

            self.accounts.user_lp = AccountMeta::new_readonly(accounts.user_lp, false);

            self.accounts.lp_user_info = AccountMeta::new(accounts.lp_user_info, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.user_o_sola = AccountMeta::new(accounts.user_o_sola, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.user_lp.clone());

            metas.push(self.accounts.lp_user_info.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.user_o_sola.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CollectToPol
    // ....................................................................

    /// Main instruction struct for CollectToPol
    pub struct CollectToPolInstruction {
        pub accounts: CollectToPolInstructionAccountMetas,
        pub data: CollectToPolInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CollectToPol instruction
    #[derive(Debug, Clone, Default)]
    pub struct CollectToPolInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pol_state: AccountMeta,

        pub market_vault: AccountMeta,

        pub pol_usdc_vault: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for CollectToPol instruction
    #[derive(Debug, Clone)]
    pub struct CollectToPolInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub pol_state: Pubkey,

        pub market_vault: Pubkey,

        pub pol_usdc_vault: Pubkey,
    }

    impl CollectToPolInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            pol_state: Pubkey,

            market_vault: Pubkey,

            pol_usdc_vault: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                pol_state,

                market_vault,

                pol_usdc_vault,
            }
        }
    }

    /// Instruction data for CollectToPol
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CollectToPolInstructionData {
        pub amount: u64,
    }

    impl CollectToPolInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for CollectToPolInstruction
    impl CollectToPolInstruction {
        fn discriminator() -> [u8; 8] {
            [107u8, 247u8, 240u8, 206u8, 210u8, 212u8, 167u8, 109u8]
        }

        pub fn data(data: CollectToPolInstructionData) -> Self {
            Self {
                accounts: CollectToPolInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CollectToPolInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.pol_state = AccountMeta::new(accounts.pol_state, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.pol_usdc_vault = AccountMeta::new(accounts.pol_usdc_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pol_state.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.pol_usdc_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ContributorBorrowUsdc
    // ....................................................................

    /// Main instruction struct for ContributorBorrowUsdc
    pub struct ContributorBorrowUsdcInstruction {
        pub accounts: ContributorBorrowUsdcInstructionAccountMetas,
        pub data: ContributorBorrowUsdcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ContributorBorrowUsdc instruction
    #[derive(Debug, Clone, Default)]
    pub struct ContributorBorrowUsdcInstructionAccountMetas {
        pub contributor: AccountMeta,

        pub protocol_state: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub contributor_usdc: AccountMeta,

        pub contributor_position: AccountMeta,

        pub contributor_vesting: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ContributorBorrowUsdc instruction
    #[derive(Debug, Clone)]
    pub struct ContributorBorrowUsdcInstructionAccounts {
        pub contributor: Pubkey,

        pub protocol_state: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,

        pub usdc_mint: Pubkey,

        pub contributor_usdc: Pubkey,

        pub contributor_position: Pubkey,

        pub contributor_vesting: Pubkey,
    }

    impl ContributorBorrowUsdcInstructionAccounts {
        pub fn new(
            contributor: Pubkey,

            protocol_state: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,

            usdc_mint: Pubkey,

            contributor_usdc: Pubkey,

            contributor_position: Pubkey,

            contributor_vesting: Pubkey,
        ) -> Self {
            Self {
                contributor,

                protocol_state,

                floor_vault,

                market_vault,

                usdc_mint,

                contributor_usdc,

                contributor_position,

                contributor_vesting,
            }
        }
    }

    /// Instruction data for ContributorBorrowUsdc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ContributorBorrowUsdcInstructionData {
        pub usdc_amount: u64,
    }

    impl ContributorBorrowUsdcInstructionData {
        pub fn new(usdc_amount: u64) -> Self {
            Self { usdc_amount }
        }
    }

    /// Implementation for ContributorBorrowUsdcInstruction
    impl ContributorBorrowUsdcInstruction {
        fn discriminator() -> [u8; 8] {
            [191u8, 82u8, 144u8, 27u8, 109u8, 56u8, 16u8, 50u8]
        }

        pub fn data(data: ContributorBorrowUsdcInstructionData) -> Self {
            Self {
                accounts: ContributorBorrowUsdcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ContributorBorrowUsdcInstructionAccounts) -> Self {
            self.accounts.contributor = AccountMeta::new(accounts.contributor, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.contributor_usdc = AccountMeta::new(accounts.contributor_usdc, false);

            self.accounts.contributor_position =
                AccountMeta::new(accounts.contributor_position, false);

            self.accounts.contributor_vesting =
                AccountMeta::new(accounts.contributor_vesting, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.contributor.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.contributor_usdc.clone());

            metas.push(self.accounts.contributor_position.clone());

            metas.push(self.accounts.contributor_vesting.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: CreatePool
    // ....................................................................

    /// Main instruction struct for CreatePool
    pub struct CreatePoolInstruction {
        pub accounts: CreatePoolInstructionAccountMetas,
        pub data: CreatePoolInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for CreatePool instruction
    #[derive(Debug, Clone, Default)]
    pub struct CreatePoolInstructionAccountMetas {
        pub creator: AccountMeta,

        pub protocol_state: AccountMeta,

        pub token_a_mint: AccountMeta,

        pub token_b_mint: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub token_a_vault: AccountMeta,

        pub token_b_vault: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for CreatePool instruction
    #[derive(Debug, Clone)]
    pub struct CreatePoolInstructionAccounts {
        pub creator: Pubkey,

        pub protocol_state: Pubkey,

        pub token_a_mint: Pubkey,

        pub token_b_mint: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub token_a_vault: Pubkey,

        pub token_b_vault: Pubkey,
    }

    impl CreatePoolInstructionAccounts {
        pub fn new(
            creator: Pubkey,

            protocol_state: Pubkey,

            token_a_mint: Pubkey,

            token_b_mint: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            token_a_vault: Pubkey,

            token_b_vault: Pubkey,
        ) -> Self {
            Self {
                creator,

                protocol_state,

                token_a_mint,

                token_b_mint,

                pool,

                lp_mint,

                token_a_vault,

                token_b_vault,
            }
        }
    }

    /// Instruction data for CreatePool
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct CreatePoolInstructionData {
        pub fee_rate: u16,

        pub protocol_fee_bps: u16,
    }

    impl CreatePoolInstructionData {
        pub fn new(fee_rate: u16, protocol_fee_bps: u16) -> Self {
            Self {
                fee_rate,

                protocol_fee_bps,
            }
        }
    }

    /// Implementation for CreatePoolInstruction
    impl CreatePoolInstruction {
        fn discriminator() -> [u8; 8] {
            [233u8, 146u8, 209u8, 142u8, 207u8, 104u8, 64u8, 188u8]
        }

        pub fn data(data: CreatePoolInstructionData) -> Self {
            Self {
                accounts: CreatePoolInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: CreatePoolInstructionAccounts) -> Self {
            self.accounts.creator = AccountMeta::new(accounts.creator, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.token_a_mint = AccountMeta::new_readonly(accounts.token_a_mint, false);

            self.accounts.token_b_mint = AccountMeta::new_readonly(accounts.token_b_mint, false);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new(accounts.lp_mint, false);

            self.accounts.token_a_vault = AccountMeta::new(accounts.token_a_vault, false);

            self.accounts.token_b_vault = AccountMeta::new(accounts.token_b_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.creator.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.token_a_mint.clone());

            metas.push(self.accounts.token_b_mint.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.token_a_vault.clone());

            metas.push(self.accounts.token_b_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: DeployPol
    // ....................................................................

    /// Main instruction struct for DeployPol
    pub struct DeployPolInstruction {
        pub accounts: DeployPolInstructionAccountMetas,
        pub data: DeployPolInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for DeployPol instruction
    #[derive(Debug, Clone, Default)]
    pub struct DeployPolInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pol_state: AccountMeta,

        pub pol_usdc_vault: AccountMeta,

        pub pol_sola_ata: AccountMeta,

        pub pol_lp_vault: AccountMeta,

        pub sola_mint: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub pool_token_a_vault: AccountMeta,

        pub pool_token_b_vault: AccountMeta,

        pub lp_dead_ata: AccountMeta,

        pub lp_dead: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for DeployPol instruction
    #[derive(Debug, Clone)]
    pub struct DeployPolInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub pol_state: Pubkey,

        pub pol_usdc_vault: Pubkey,

        pub pol_sola_ata: Pubkey,

        pub pol_lp_vault: Pubkey,

        pub sola_mint: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub pool_token_a_vault: Pubkey,

        pub pool_token_b_vault: Pubkey,

        pub lp_dead_ata: Pubkey,
    }

    impl DeployPolInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            pol_state: Pubkey,

            pol_usdc_vault: Pubkey,

            pol_sola_ata: Pubkey,

            pol_lp_vault: Pubkey,

            sola_mint: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            pool_token_a_vault: Pubkey,

            pool_token_b_vault: Pubkey,

            lp_dead_ata: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                pol_state,

                pol_usdc_vault,

                pol_sola_ata,

                pol_lp_vault,

                sola_mint,

                floor_vault,

                market_vault,

                pool,

                lp_mint,

                pool_token_a_vault,

                pool_token_b_vault,

                lp_dead_ata,
            }
        }
    }

    /// Instruction data for DeployPol
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DeployPolInstructionData {
        pub usdc_for_sola: u64,

        pub min_sola_out: u64,

        pub sola_for_lp: u64,

        pub usdc_for_lp: u64,

        pub min_lp: u64,
    }

    impl DeployPolInstructionData {
        pub fn new(
            usdc_for_sola: u64,

            min_sola_out: u64,

            sola_for_lp: u64,

            usdc_for_lp: u64,

            min_lp: u64,
        ) -> Self {
            Self {
                usdc_for_sola,

                min_sola_out,

                sola_for_lp,

                usdc_for_lp,

                min_lp,
            }
        }
    }

    /// Implementation for DeployPolInstruction
    impl DeployPolInstruction {
        fn discriminator() -> [u8; 8] {
            [110u8, 232u8, 26u8, 24u8, 49u8, 72u8, 56u8, 103u8]
        }

        pub fn data(data: DeployPolInstructionData) -> Self {
            Self {
                accounts: DeployPolInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DeployPolInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.pol_state = AccountMeta::new_readonly(accounts.pol_state, false);

            self.accounts.pol_usdc_vault = AccountMeta::new(accounts.pol_usdc_vault, false);

            self.accounts.pol_sola_ata = AccountMeta::new(accounts.pol_sola_ata, false);

            self.accounts.pol_lp_vault = AccountMeta::new(accounts.pol_lp_vault, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new(accounts.lp_mint, false);

            self.accounts.pool_token_a_vault = AccountMeta::new(accounts.pool_token_a_vault, false);

            self.accounts.pool_token_b_vault = AccountMeta::new(accounts.pool_token_b_vault, false);

            self.accounts.lp_dead_ata = AccountMeta::new(accounts.lp_dead_ata, false);

            self.accounts.lp_dead =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pol_state.clone());

            metas.push(self.accounts.pol_usdc_vault.clone());

            metas.push(self.accounts.pol_sola_ata.clone());

            metas.push(self.accounts.pol_lp_vault.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.pool_token_a_vault.clone());

            metas.push(self.accounts.pool_token_b_vault.clone());

            metas.push(self.accounts.lp_dead_ata.clone());

            metas.push(self.accounts.lp_dead.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: DepositBribe
    // ....................................................................

    /// Main instruction struct for DepositBribe
    pub struct DepositBribeInstruction {
        pub accounts: DepositBribeInstructionAccountMetas,
        pub data: DepositBribeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for DepositBribe instruction
    #[derive(Debug, Clone, Default)]
    pub struct DepositBribeInstructionAccountMetas {
        pub depositor: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pool_id: AccountMeta,

        pub reward_mint: AccountMeta,

        pub depositor_token: AccountMeta,

        pub bribe_vault: AccountMeta,

        pub bribe_token_vault: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for DepositBribe instruction
    #[derive(Debug, Clone)]
    pub struct DepositBribeInstructionAccounts {
        pub depositor: Pubkey,

        pub protocol_state: Pubkey,

        pub pool_id: Pubkey,

        pub reward_mint: Pubkey,

        pub depositor_token: Pubkey,

        pub bribe_vault: Pubkey,

        pub bribe_token_vault: Pubkey,
    }

    impl DepositBribeInstructionAccounts {
        pub fn new(
            depositor: Pubkey,

            protocol_state: Pubkey,

            pool_id: Pubkey,

            reward_mint: Pubkey,

            depositor_token: Pubkey,

            bribe_vault: Pubkey,

            bribe_token_vault: Pubkey,
        ) -> Self {
            Self {
                depositor,

                protocol_state,

                pool_id,

                reward_mint,

                depositor_token,

                bribe_vault,

                bribe_token_vault,
            }
        }
    }

    /// Instruction data for DepositBribe
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DepositBribeInstructionData {
        pub epoch: u64,

        pub amount: u64,
    }

    impl DepositBribeInstructionData {
        pub fn new(epoch: u64, amount: u64) -> Self {
            Self { epoch, amount }
        }
    }

    /// Implementation for DepositBribeInstruction
    impl DepositBribeInstruction {
        fn discriminator() -> [u8; 8] {
            [159u8, 44u8, 249u8, 110u8, 78u8, 209u8, 126u8, 9u8]
        }

        pub fn data(data: DepositBribeInstructionData) -> Self {
            Self {
                accounts: DepositBribeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DepositBribeInstructionAccounts) -> Self {
            self.accounts.depositor = AccountMeta::new(accounts.depositor, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.pool_id = AccountMeta::new_readonly(accounts.pool_id, false);

            self.accounts.reward_mint = AccountMeta::new_readonly(accounts.reward_mint, false);

            self.accounts.depositor_token = AccountMeta::new(accounts.depositor_token, false);

            self.accounts.bribe_vault = AccountMeta::new(accounts.bribe_vault, false);

            self.accounts.bribe_token_vault = AccountMeta::new(accounts.bribe_token_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.depositor.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pool_id.clone());

            metas.push(self.accounts.reward_mint.clone());

            metas.push(self.accounts.depositor_token.clone());

            metas.push(self.accounts.bribe_vault.clone());

            metas.push(self.accounts.bribe_token_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: DistributeOSola
    // ....................................................................

    /// Main instruction struct for DistributeOSola
    pub struct DistributeOSolaInstruction {
        pub accounts: DistributeOSolaInstructionAccountMetas,
        pub data: DistributeOSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for DistributeOSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct DistributeOSolaInstructionAccountMetas {
        pub authority: AccountMeta,

        pub recipient: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub recipient_o_sola: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for DistributeOSola instruction
    #[derive(Debug, Clone)]
    pub struct DistributeOSolaInstructionAccounts {
        pub authority: Pubkey,

        pub recipient: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub recipient_o_sola: Pubkey,
    }

    impl DistributeOSolaInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            recipient: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            recipient_o_sola: Pubkey,
        ) -> Self {
            Self {
                authority,

                recipient,

                protocol_state,

                o_sola_mint,

                recipient_o_sola,
            }
        }
    }

    /// Instruction data for DistributeOSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct DistributeOSolaInstructionData {
        pub amount: u64,
    }

    impl DistributeOSolaInstructionData {
        pub fn new(amount: u64) -> Self {
            Self { amount }
        }
    }

    /// Implementation for DistributeOSolaInstruction
    impl DistributeOSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 105u8, 50u8, 201u8, 210u8, 162u8, 187u8, 14u8]
        }

        pub fn data(data: DistributeOSolaInstructionData) -> Self {
            Self {
                accounts: DistributeOSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: DistributeOSolaInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.recipient = AccountMeta::new_readonly(accounts.recipient, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.recipient_o_sola = AccountMeta::new(accounts.recipient_o_sola, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.recipient.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.recipient_o_sola.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: EmitPoolRewards
    // ....................................................................

    /// Main instruction struct for EmitPoolRewards
    pub struct EmitPoolRewardsInstruction {
        pub accounts: EmitPoolRewardsInstructionAccountMetas,
        pub data: EmitPoolRewardsInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for EmitPoolRewards instruction
    #[derive(Debug, Clone, Default)]
    pub struct EmitPoolRewardsInstructionAccountMetas {
        pub caller: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub gauge_state: AccountMeta,

        pub global_epoch_votes: AccountMeta,

        pub pool_epoch_accum: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for EmitPoolRewards instruction
    #[derive(Debug, Clone)]
    pub struct EmitPoolRewardsInstructionAccounts {
        pub caller: Pubkey,

        pub protocol_state: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub gauge_state: Pubkey,

        pub global_epoch_votes: Pubkey,

        pub pool_epoch_accum: Pubkey,
    }

    impl EmitPoolRewardsInstructionAccounts {
        pub fn new(
            caller: Pubkey,

            protocol_state: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            gauge_state: Pubkey,

            global_epoch_votes: Pubkey,

            pool_epoch_accum: Pubkey,
        ) -> Self {
            Self {
                caller,

                protocol_state,

                pool,

                lp_mint,

                gauge_state,

                global_epoch_votes,

                pool_epoch_accum,
            }
        }
    }

    /// Instruction data for EmitPoolRewards
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct EmitPoolRewardsInstructionData {
        pub epoch: u64,
    }

    impl EmitPoolRewardsInstructionData {
        pub fn new(epoch: u64) -> Self {
            Self { epoch }
        }
    }

    /// Implementation for EmitPoolRewardsInstruction
    impl EmitPoolRewardsInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 4u8, 186u8, 137u8, 139u8, 70u8, 160u8, 112u8]
        }

        pub fn data(data: EmitPoolRewardsInstructionData) -> Self {
            Self {
                accounts: EmitPoolRewardsInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: EmitPoolRewardsInstructionAccounts) -> Self {
            self.accounts.caller = AccountMeta::new(accounts.caller, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.pool = AccountMeta::new_readonly(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new_readonly(accounts.lp_mint, false);

            self.accounts.gauge_state = AccountMeta::new_readonly(accounts.gauge_state, false);

            self.accounts.global_epoch_votes =
                AccountMeta::new_readonly(accounts.global_epoch_votes, false);

            self.accounts.pool_epoch_accum = AccountMeta::new(accounts.pool_epoch_accum, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.caller.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.gauge_state.clone());

            metas.push(self.accounts.global_epoch_votes.clone());

            metas.push(self.accounts.pool_epoch_accum.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: ExerciseOSola
    // ....................................................................

    /// Main instruction struct for ExerciseOSola
    pub struct ExerciseOSolaInstruction {
        pub accounts: ExerciseOSolaInstructionAccountMetas,
        pub data: ExerciseOSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for ExerciseOSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct ExerciseOSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub user_o_sola: AccountMeta,

        pub user_sola: AccountMeta,

        pub floor_vault: AccountMeta,

        pub user_usdc: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for ExerciseOSola instruction
    #[derive(Debug, Clone)]
    pub struct ExerciseOSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub o_sola_mint: Pubkey,

        pub user_o_sola: Pubkey,

        pub user_sola: Pubkey,

        pub floor_vault: Pubkey,

        pub user_usdc: Pubkey,
    }

    impl ExerciseOSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            o_sola_mint: Pubkey,

            user_o_sola: Pubkey,

            user_sola: Pubkey,

            floor_vault: Pubkey,

            user_usdc: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                sola_mint,

                o_sola_mint,

                user_o_sola,

                user_sola,

                floor_vault,

                user_usdc,
            }
        }
    }

    /// Instruction data for ExerciseOSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct ExerciseOSolaInstructionData {
        pub o_sola_amount: u64,
    }

    impl ExerciseOSolaInstructionData {
        pub fn new(o_sola_amount: u64) -> Self {
            Self { o_sola_amount }
        }
    }

    /// Implementation for ExerciseOSolaInstruction
    impl ExerciseOSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [74u8, 214u8, 117u8, 160u8, 171u8, 161u8, 126u8, 242u8]
        }

        pub fn data(data: ExerciseOSolaInstructionData) -> Self {
            Self {
                accounts: ExerciseOSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: ExerciseOSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.user_o_sola = AccountMeta::new(accounts.user_o_sola, false);

            self.accounts.user_sola = AccountMeta::new(accounts.user_sola, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.user_o_sola.clone());

            metas.push(self.accounts.user_sola.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: FlashArbitrage
    // ....................................................................

    /// Main instruction struct for FlashArbitrage
    pub struct FlashArbitrageInstruction {
        pub accounts: FlashArbitrageInstructionAccountMetas,
        pub data: FlashArbitrageInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for FlashArbitrage instruction
    #[derive(Debug, Clone, Default)]
    pub struct FlashArbitrageInstructionAccountMetas {
        pub caller: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub sola_mint: AccountMeta,

        pub caller_o_sola: AccountMeta,

        pub caller_sola: AccountMeta,

        pub caller_usdc: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub pool: AccountMeta,

        pub token_a_vault: AccountMeta,

        pub token_b_vault: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for FlashArbitrage instruction
    #[derive(Debug, Clone)]
    pub struct FlashArbitrageInstructionAccounts {
        pub caller: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub sola_mint: Pubkey,

        pub caller_o_sola: Pubkey,

        pub caller_sola: Pubkey,

        pub caller_usdc: Pubkey,

        pub usdc_mint: Pubkey,

        pub pool: Pubkey,

        pub token_a_vault: Pubkey,

        pub token_b_vault: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,
    }

    impl FlashArbitrageInstructionAccounts {
        pub fn new(
            caller: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            sola_mint: Pubkey,

            caller_o_sola: Pubkey,

            caller_sola: Pubkey,

            caller_usdc: Pubkey,

            usdc_mint: Pubkey,

            pool: Pubkey,

            token_a_vault: Pubkey,

            token_b_vault: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,
        ) -> Self {
            Self {
                caller,

                protocol_state,

                o_sola_mint,

                sola_mint,

                caller_o_sola,

                caller_sola,

                caller_usdc,

                usdc_mint,

                pool,

                token_a_vault,

                token_b_vault,

                floor_vault,

                market_vault,
            }
        }
    }

    /// Instruction data for FlashArbitrage
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct FlashArbitrageInstructionData {
        pub amount_osola: u64,

        pub min_profit_usdc: u64,
    }

    impl FlashArbitrageInstructionData {
        pub fn new(amount_osola: u64, min_profit_usdc: u64) -> Self {
            Self {
                amount_osola,

                min_profit_usdc,
            }
        }
    }

    /// Implementation for FlashArbitrageInstruction
    impl FlashArbitrageInstruction {
        fn discriminator() -> [u8; 8] {
            [153u8, 26u8, 38u8, 120u8, 121u8, 43u8, 168u8, 104u8]
        }

        pub fn data(data: FlashArbitrageInstructionData) -> Self {
            Self {
                accounts: FlashArbitrageInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: FlashArbitrageInstructionAccounts) -> Self {
            self.accounts.caller = AccountMeta::new(accounts.caller, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.caller_o_sola = AccountMeta::new(accounts.caller_o_sola, false);

            self.accounts.caller_sola = AccountMeta::new(accounts.caller_sola, false);

            self.accounts.caller_usdc = AccountMeta::new(accounts.caller_usdc, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.token_a_vault = AccountMeta::new(accounts.token_a_vault, false);

            self.accounts.token_b_vault = AccountMeta::new(accounts.token_b_vault, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.caller.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.caller_o_sola.clone());

            metas.push(self.accounts.caller_sola.clone());

            metas.push(self.accounts.caller_usdc.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.token_a_vault.clone());

            metas.push(self.accounts.token_b_vault.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: FounderBorrowUsdc
    // ....................................................................

    /// Main instruction struct for FounderBorrowUsdc
    pub struct FounderBorrowUsdcInstruction {
        pub accounts: FounderBorrowUsdcInstructionAccountMetas,
        pub data: FounderBorrowUsdcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for FounderBorrowUsdc instruction
    #[derive(Debug, Clone, Default)]
    pub struct FounderBorrowUsdcInstructionAccountMetas {
        pub founder: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub founder_hi_sola: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub founder_usdc: AccountMeta,

        pub founder_position: AccountMeta,

        pub founder_hi_vesting: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for FounderBorrowUsdc instruction
    #[derive(Debug, Clone)]
    pub struct FounderBorrowUsdcInstructionAccounts {
        pub founder: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub founder_hi_sola: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,

        pub usdc_mint: Pubkey,

        pub founder_usdc: Pubkey,

        pub founder_position: Pubkey,

        pub founder_hi_vesting: Pubkey,
    }

    impl FounderBorrowUsdcInstructionAccounts {
        pub fn new(
            founder: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            founder_hi_sola: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,

            usdc_mint: Pubkey,

            founder_usdc: Pubkey,

            founder_position: Pubkey,

            founder_hi_vesting: Pubkey,
        ) -> Self {
            Self {
                founder,

                protocol_state,

                hi_sola_mint,

                founder_hi_sola,

                floor_vault,

                market_vault,

                usdc_mint,

                founder_usdc,

                founder_position,

                founder_hi_vesting,
            }
        }
    }

    /// Instruction data for FounderBorrowUsdc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct FounderBorrowUsdcInstructionData {
        pub usdc_amount: u64,
    }

    impl FounderBorrowUsdcInstructionData {
        pub fn new(usdc_amount: u64) -> Self {
            Self { usdc_amount }
        }
    }

    /// Implementation for FounderBorrowUsdcInstruction
    impl FounderBorrowUsdcInstruction {
        fn discriminator() -> [u8; 8] {
            [5u8, 196u8, 173u8, 39u8, 65u8, 107u8, 159u8, 237u8]
        }

        pub fn data(data: FounderBorrowUsdcInstructionData) -> Self {
            Self {
                accounts: FounderBorrowUsdcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: FounderBorrowUsdcInstructionAccounts) -> Self {
            self.accounts.founder = AccountMeta::new(accounts.founder, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.founder_hi_sola =
                AccountMeta::new_readonly(accounts.founder_hi_sola, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.founder_usdc = AccountMeta::new(accounts.founder_usdc, false);

            self.accounts.founder_position = AccountMeta::new(accounts.founder_position, false);

            self.accounts.founder_hi_vesting =
                AccountMeta::new_readonly(accounts.founder_hi_vesting, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.founder.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.founder_hi_sola.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.founder_usdc.clone());

            metas.push(self.accounts.founder_position.clone());

            metas.push(self.accounts.founder_hi_vesting.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Initialize
    // ....................................................................

    /// Main instruction struct for Initialize
    pub struct InitializeInstruction {
        pub accounts: InitializeInstructionAccountMetas,
        pub data: InitializeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Initialize instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializeInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub sola_mint: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub floor_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub sola_vault: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for Initialize instruction
    #[derive(Debug, Clone)]
    pub struct InitializeInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub usdc_mint: Pubkey,

        pub sola_mint: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub o_sola_mint: Pubkey,

        pub floor_vault: Pubkey,

        pub market_vault: Pubkey,

        pub sola_vault: Pubkey,
    }

    impl InitializeInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            usdc_mint: Pubkey,

            sola_mint: Pubkey,

            hi_sola_mint: Pubkey,

            o_sola_mint: Pubkey,

            floor_vault: Pubkey,

            market_vault: Pubkey,

            sola_vault: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                usdc_mint,

                sola_mint,

                hi_sola_mint,

                o_sola_mint,

                floor_vault,

                market_vault,

                sola_vault,
            }
        }
    }

    /// Instruction data for Initialize
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializeInstructionData {}

    impl InitializeInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for InitializeInstruction
    impl InitializeInstruction {
        fn discriminator() -> [u8; 8] {
            [175u8, 175u8, 109u8, 31u8, 13u8, 152u8, 155u8, 237u8]
        }

        pub fn data(data: InitializeInstructionData) -> Self {
            Self {
                accounts: InitializeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializeInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.hi_sola_mint = AccountMeta::new(accounts.hi_sola_mint, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.sola_vault = AccountMeta::new(accounts.sola_vault, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.sola_vault.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: InitializePol
    // ....................................................................

    /// Main instruction struct for InitializePol
    pub struct InitializePolInstruction {
        pub accounts: InitializePolInstructionAccountMetas,
        pub data: InitializePolInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for InitializePol instruction
    #[derive(Debug, Clone, Default)]
    pub struct InitializePolInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub pol_state: AccountMeta,

        pub pol_usdc_vault: AccountMeta,

        pub pol_sola_ata: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub sola_mint: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for InitializePol instruction
    #[derive(Debug, Clone)]
    pub struct InitializePolInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub pol_state: Pubkey,

        pub pol_usdc_vault: Pubkey,

        pub pol_sola_ata: Pubkey,

        pub usdc_mint: Pubkey,

        pub sola_mint: Pubkey,
    }

    impl InitializePolInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            pol_state: Pubkey,

            pol_usdc_vault: Pubkey,

            pol_sola_ata: Pubkey,

            usdc_mint: Pubkey,

            sola_mint: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                pol_state,

                pol_usdc_vault,

                pol_sola_ata,

                usdc_mint,

                sola_mint,
            }
        }
    }

    /// Instruction data for InitializePol
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct InitializePolInstructionData {
        pub pol_split_bps: u16,

        pub target_pool: Pubkey,
    }

    impl InitializePolInstructionData {
        pub fn new(pol_split_bps: u16, target_pool: Pubkey) -> Self {
            Self {
                pol_split_bps,

                target_pool,
            }
        }
    }

    /// Implementation for InitializePolInstruction
    impl InitializePolInstruction {
        fn discriminator() -> [u8; 8] {
            [153u8, 249u8, 139u8, 4u8, 23u8, 76u8, 102u8, 244u8]
        }

        pub fn data(data: InitializePolInstructionData) -> Self {
            Self {
                accounts: InitializePolInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: InitializePolInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.pol_state = AccountMeta::new(accounts.pol_state, false);

            self.accounts.pol_usdc_vault = AccountMeta::new(accounts.pol_usdc_vault, false);

            self.accounts.pol_sola_ata = AccountMeta::new(accounts.pol_sola_ata, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.sola_mint = AccountMeta::new_readonly(accounts.sola_mint, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.pol_state.clone());

            metas.push(self.accounts.pol_usdc_vault.clone());

            metas.push(self.accounts.pol_sola_ata.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: LockHiSola
    // ....................................................................

    /// Main instruction struct for LockHiSola
    pub struct LockHiSolaInstruction {
        pub accounts: LockHiSolaInstructionAccountMetas,
        pub data: LockHiSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for LockHiSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct LockHiSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub lock_position: AccountMeta,

        pub ve_lock_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for LockHiSola instruction
    #[derive(Debug, Clone)]
    pub struct LockHiSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub lock_position: Pubkey,

        pub ve_lock_vault: Pubkey,

        pub market_vault: Pubkey,

        pub user_position: Pubkey,
    }

    impl LockHiSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            lock_position: Pubkey,

            ve_lock_vault: Pubkey,

            market_vault: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                hi_sola_mint,

                user_hi_sola,

                lock_position,

                ve_lock_vault,

                market_vault,

                user_position,
            }
        }
    }

    /// Instruction data for LockHiSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct LockHiSolaInstructionData {
        pub amount: u64,

        pub lock_duration_secs: u64,
    }

    impl LockHiSolaInstructionData {
        pub fn new(amount: u64, lock_duration_secs: u64) -> Self {
            Self {
                amount,

                lock_duration_secs,
            }
        }
    }

    /// Implementation for LockHiSolaInstruction
    impl LockHiSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [149u8, 150u8, 137u8, 146u8, 182u8, 91u8, 45u8, 31u8]
        }

        pub fn data(data: LockHiSolaInstructionData) -> Self {
            Self {
                accounts: LockHiSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: LockHiSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new(accounts.user_hi_sola, false);

            self.accounts.lock_position = AccountMeta::new(accounts.lock_position, false);

            self.accounts.ve_lock_vault = AccountMeta::new(accounts.ve_lock_vault, false);

            self.accounts.market_vault = AccountMeta::new_readonly(accounts.market_vault, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.lock_position.clone());

            metas.push(self.accounts.ve_lock_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MintEcosystemAllocation
    // ....................................................................

    /// Main instruction struct for MintEcosystemAllocation
    pub struct MintEcosystemAllocationInstruction {
        pub accounts: MintEcosystemAllocationInstructionAccountMetas,
        pub data: MintEcosystemAllocationInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MintEcosystemAllocation instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintEcosystemAllocationInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub authority_sola: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for MintEcosystemAllocation instruction
    #[derive(Debug, Clone)]
    pub struct MintEcosystemAllocationInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub authority_sola: Pubkey,
    }

    impl MintEcosystemAllocationInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            authority_sola: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                sola_mint,

                authority_sola,
            }
        }
    }

    /// Instruction data for MintEcosystemAllocation
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintEcosystemAllocationInstructionData {}

    impl MintEcosystemAllocationInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for MintEcosystemAllocationInstruction
    impl MintEcosystemAllocationInstruction {
        fn discriminator() -> [u8; 8] {
            [103u8, 83u8, 134u8, 227u8, 250u8, 112u8, 218u8, 116u8]
        }

        pub fn data(data: MintEcosystemAllocationInstructionData) -> Self {
            Self {
                accounts: MintEcosystemAllocationInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintEcosystemAllocationInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.authority_sola = AccountMeta::new(accounts.authority_sola, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.authority_sola.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: MintFounderAllocation
    // ....................................................................

    /// Main instruction struct for MintFounderAllocation
    pub struct MintFounderAllocationInstruction {
        pub accounts: MintFounderAllocationInstructionAccountMetas,
        pub data: MintFounderAllocationInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for MintFounderAllocation instruction
    #[derive(Debug, Clone, Default)]
    pub struct MintFounderAllocationInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub founder: AccountMeta,

        pub founder_hi_vesting: AccountMeta,

        pub founder_vesting: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for MintFounderAllocation instruction
    #[derive(Debug, Clone)]
    pub struct MintFounderAllocationInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub founder: Pubkey,

        pub founder_hi_vesting: Pubkey,

        pub founder_vesting: Pubkey,
    }

    impl MintFounderAllocationInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            founder: Pubkey,

            founder_hi_vesting: Pubkey,

            founder_vesting: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                founder,

                founder_hi_vesting,

                founder_vesting,
            }
        }
    }

    /// Instruction data for MintFounderAllocation
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct MintFounderAllocationInstructionData {}

    impl MintFounderAllocationInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for MintFounderAllocationInstruction
    impl MintFounderAllocationInstruction {
        fn discriminator() -> [u8; 8] {
            [254u8, 75u8, 254u8, 93u8, 237u8, 113u8, 196u8, 137u8]
        }

        pub fn data(data: MintFounderAllocationInstructionData) -> Self {
            Self {
                accounts: MintFounderAllocationInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: MintFounderAllocationInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.founder = AccountMeta::new_readonly(accounts.founder, false);

            self.accounts.founder_hi_vesting = AccountMeta::new(accounts.founder_hi_vesting, false);

            self.accounts.founder_vesting = AccountMeta::new(accounts.founder_vesting, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.founder.clone());

            metas.push(self.accounts.founder_hi_vesting.clone());

            metas.push(self.accounts.founder_vesting.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Pause
    // ....................................................................

    /// Main instruction struct for Pause
    pub struct PauseInstruction {
        pub accounts: PauseInstructionAccountMetas,
        pub data: PauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Pause instruction
    #[derive(Debug, Clone, Default)]
    pub struct PauseInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,
    }

    /// Account pubkeys for Pause instruction
    #[derive(Debug, Clone)]
    pub struct PauseInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,
    }

    impl PauseInstructionAccounts {
        pub fn new(authority: Pubkey, protocol_state: Pubkey) -> Self {
            Self {
                authority,

                protocol_state,
            }
        }
    }

    /// Instruction data for Pause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct PauseInstructionData {}

    impl PauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for PauseInstruction
    impl PauseInstruction {
        fn discriminator() -> [u8; 8] {
            [211u8, 22u8, 221u8, 251u8, 74u8, 121u8, 193u8, 47u8]
        }

        pub fn data(data: PauseInstructionData) -> Self {
            Self {
                accounts: PauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: PauseInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RegisterContributor
    // ....................................................................

    /// Main instruction struct for RegisterContributor
    pub struct RegisterContributorInstruction {
        pub accounts: RegisterContributorInstructionAccountMetas,
        pub data: RegisterContributorInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RegisterContributor instruction
    #[derive(Debug, Clone, Default)]
    pub struct RegisterContributorInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,

        pub contributor_wallet: AccountMeta,

        pub contributor_vesting: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for RegisterContributor instruction
    #[derive(Debug, Clone)]
    pub struct RegisterContributorInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,

        pub contributor_wallet: Pubkey,

        pub contributor_vesting: Pubkey,
    }

    impl RegisterContributorInstructionAccounts {
        pub fn new(
            authority: Pubkey,

            protocol_state: Pubkey,

            contributor_wallet: Pubkey,

            contributor_vesting: Pubkey,
        ) -> Self {
            Self {
                authority,

                protocol_state,

                contributor_wallet,

                contributor_vesting,
            }
        }
    }

    /// Instruction data for RegisterContributor
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RegisterContributorInstructionData {
        pub hi_sola_amount: u64,

        pub o_sola_amount: u64,
    }

    impl RegisterContributorInstructionData {
        pub fn new(hi_sola_amount: u64, o_sola_amount: u64) -> Self {
            Self {
                hi_sola_amount,

                o_sola_amount,
            }
        }
    }

    /// Implementation for RegisterContributorInstruction
    impl RegisterContributorInstruction {
        fn discriminator() -> [u8; 8] {
            [14u8, 116u8, 59u8, 252u8, 26u8, 253u8, 91u8, 175u8]
        }

        pub fn data(data: RegisterContributorInstructionData) -> Self {
            Self {
                accounts: RegisterContributorInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RegisterContributorInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new(accounts.authority, true);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.contributor_wallet =
                AccountMeta::new_readonly(accounts.contributor_wallet, false);

            self.accounts.contributor_vesting =
                AccountMeta::new(accounts.contributor_vesting, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.contributor_wallet.clone());

            metas.push(self.accounts.contributor_vesting.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RemoveLiquidity
    // ....................................................................

    /// Main instruction struct for RemoveLiquidity
    pub struct RemoveLiquidityInstruction {
        pub accounts: RemoveLiquidityInstructionAccountMetas,
        pub data: RemoveLiquidityInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RemoveLiquidity instruction
    #[derive(Debug, Clone, Default)]
    pub struct RemoveLiquidityInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool: AccountMeta,

        pub lp_mint: AccountMeta,

        pub token_a_vault: AccountMeta,

        pub token_b_vault: AccountMeta,

        pub user_lp: AccountMeta,

        pub user_token_a: AccountMeta,

        pub user_token_b: AccountMeta,

        pub lp_user_info: AccountMeta,

        pub protocol_state: AccountMeta,

        pub o_sola_mint: AccountMeta,

        pub user_o_sola: AccountMeta,

        pub rent: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for RemoveLiquidity instruction
    #[derive(Debug, Clone)]
    pub struct RemoveLiquidityInstructionAccounts {
        pub user: Pubkey,

        pub pool: Pubkey,

        pub lp_mint: Pubkey,

        pub token_a_vault: Pubkey,

        pub token_b_vault: Pubkey,

        pub user_lp: Pubkey,

        pub user_token_a: Pubkey,

        pub user_token_b: Pubkey,

        pub lp_user_info: Pubkey,

        pub protocol_state: Pubkey,

        pub o_sola_mint: Pubkey,

        pub user_o_sola: Pubkey,
    }

    impl RemoveLiquidityInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool: Pubkey,

            lp_mint: Pubkey,

            token_a_vault: Pubkey,

            token_b_vault: Pubkey,

            user_lp: Pubkey,

            user_token_a: Pubkey,

            user_token_b: Pubkey,

            lp_user_info: Pubkey,

            protocol_state: Pubkey,

            o_sola_mint: Pubkey,

            user_o_sola: Pubkey,
        ) -> Self {
            Self {
                user,

                pool,

                lp_mint,

                token_a_vault,

                token_b_vault,

                user_lp,

                user_token_a,

                user_token_b,

                lp_user_info,

                protocol_state,

                o_sola_mint,

                user_o_sola,
            }
        }
    }

    /// Instruction data for RemoveLiquidity
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RemoveLiquidityInstructionData {
        pub lp_amount: u64,

        pub min_a: u64,

        pub min_b: u64,
    }

    impl RemoveLiquidityInstructionData {
        pub fn new(lp_amount: u64, min_a: u64, min_b: u64) -> Self {
            Self {
                lp_amount,

                min_a,

                min_b,
            }
        }
    }

    /// Implementation for RemoveLiquidityInstruction
    impl RemoveLiquidityInstruction {
        fn discriminator() -> [u8; 8] {
            [80u8, 85u8, 209u8, 72u8, 24u8, 206u8, 177u8, 108u8]
        }

        pub fn data(data: RemoveLiquidityInstructionData) -> Self {
            Self {
                accounts: RemoveLiquidityInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RemoveLiquidityInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool = AccountMeta::new(accounts.pool, false);

            self.accounts.lp_mint = AccountMeta::new(accounts.lp_mint, false);

            self.accounts.token_a_vault = AccountMeta::new(accounts.token_a_vault, false);

            self.accounts.token_b_vault = AccountMeta::new(accounts.token_b_vault, false);

            self.accounts.user_lp = AccountMeta::new(accounts.user_lp, false);

            self.accounts.user_token_a = AccountMeta::new(accounts.user_token_a, false);

            self.accounts.user_token_b = AccountMeta::new(accounts.user_token_b, false);

            self.accounts.lp_user_info = AccountMeta::new(accounts.lp_user_info, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.o_sola_mint = AccountMeta::new(accounts.o_sola_mint, false);

            self.accounts.user_o_sola = AccountMeta::new(accounts.user_o_sola, false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool.clone());

            metas.push(self.accounts.lp_mint.clone());

            metas.push(self.accounts.token_a_vault.clone());

            metas.push(self.accounts.token_b_vault.clone());

            metas.push(self.accounts.user_lp.clone());

            metas.push(self.accounts.user_token_a.clone());

            metas.push(self.accounts.user_token_b.clone());

            metas.push(self.accounts.lp_user_info.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.o_sola_mint.clone());

            metas.push(self.accounts.user_o_sola.clone());

            metas.push(self.accounts.rent.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: RepayUsdc
    // ....................................................................

    /// Main instruction struct for RepayUsdc
    pub struct RepayUsdcInstruction {
        pub accounts: RepayUsdcInstructionAccountMetas,
        pub data: RepayUsdcInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for RepayUsdc instruction
    #[derive(Debug, Clone, Default)]
    pub struct RepayUsdcInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub user_position: AccountMeta,

        pub floor_vault: AccountMeta,

        pub user_usdc: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for RepayUsdc instruction
    #[derive(Debug, Clone)]
    pub struct RepayUsdcInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub user_position: Pubkey,

        pub floor_vault: Pubkey,

        pub user_usdc: Pubkey,
    }

    impl RepayUsdcInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            user_position: Pubkey,

            floor_vault: Pubkey,

            user_usdc: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                user_position,

                floor_vault,

                user_usdc,
            }
        }
    }

    /// Instruction data for RepayUsdc
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct RepayUsdcInstructionData {
        pub usdc_amount: u64,
    }

    impl RepayUsdcInstructionData {
        pub fn new(usdc_amount: u64) -> Self {
            Self { usdc_amount }
        }
    }

    /// Implementation for RepayUsdcInstruction
    impl RepayUsdcInstruction {
        fn discriminator() -> [u8; 8] {
            [20u8, 104u8, 199u8, 119u8, 196u8, 22u8, 114u8, 79u8]
        }

        pub fn data(data: RepayUsdcInstructionData) -> Self {
            Self {
                accounts: RepayUsdcInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: RepayUsdcInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new_readonly(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: SellSola
    // ....................................................................

    /// Main instruction struct for SellSola
    pub struct SellSolaInstruction {
        pub accounts: SellSolaInstructionAccountMetas,
        pub data: SellSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for SellSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct SellSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub user_sola: AccountMeta,

        pub floor_vault: AccountMeta,

        pub user_usdc: AccountMeta,

        pub token_program: AccountMeta,
    }

    /// Account pubkeys for SellSola instruction
    #[derive(Debug, Clone)]
    pub struct SellSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub user_sola: Pubkey,

        pub floor_vault: Pubkey,

        pub user_usdc: Pubkey,
    }

    impl SellSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            user_sola: Pubkey,

            floor_vault: Pubkey,

            user_usdc: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                sola_mint,

                user_sola,

                floor_vault,

                user_usdc,
            }
        }
    }

    /// Instruction data for SellSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct SellSolaInstructionData {
        pub sola_amount: u64,
    }

    impl SellSolaInstructionData {
        pub fn new(sola_amount: u64) -> Self {
            Self { sola_amount }
        }
    }

    /// Implementation for SellSolaInstruction
    impl SellSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [20u8, 135u8, 8u8, 171u8, 96u8, 67u8, 223u8, 212u8]
        }

        pub fn data(data: SellSolaInstructionData) -> Self {
            Self {
                accounts: SellSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: SellSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new_readonly(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.user_sola = AccountMeta::new(accounts.user_sola, false);

            self.accounts.floor_vault = AccountMeta::new(accounts.floor_vault, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.user_sola.clone());

            metas.push(self.accounts.floor_vault.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.token_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: StakeSola
    // ....................................................................

    /// Main instruction struct for StakeSola
    pub struct StakeSolaInstruction {
        pub accounts: StakeSolaInstructionAccountMetas,
        pub data: StakeSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for StakeSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct StakeSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_sola: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub sola_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for StakeSola instruction
    #[derive(Debug, Clone)]
    pub struct StakeSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_sola: Pubkey,

        pub user_hi_sola: Pubkey,

        pub sola_vault: Pubkey,

        pub market_vault: Pubkey,

        pub user_position: Pubkey,
    }

    impl StakeSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            hi_sola_mint: Pubkey,

            user_sola: Pubkey,

            user_hi_sola: Pubkey,

            sola_vault: Pubkey,

            market_vault: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                sola_mint,

                hi_sola_mint,

                user_sola,

                user_hi_sola,

                sola_vault,

                market_vault,

                user_position,
            }
        }
    }

    /// Instruction data for StakeSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct StakeSolaInstructionData {
        pub sola_amount: u64,
    }

    impl StakeSolaInstructionData {
        pub fn new(sola_amount: u64) -> Self {
            Self { sola_amount }
        }
    }

    /// Implementation for StakeSolaInstruction
    impl StakeSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [225u8, 36u8, 239u8, 184u8, 179u8, 136u8, 230u8, 8u8]
        }

        pub fn data(data: StakeSolaInstructionData) -> Self {
            Self {
                accounts: StakeSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: StakeSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.hi_sola_mint = AccountMeta::new(accounts.hi_sola_mint, false);

            self.accounts.user_sola = AccountMeta::new(accounts.user_sola, false);

            self.accounts.user_hi_sola = AccountMeta::new(accounts.user_hi_sola, false);

            self.accounts.sola_vault = AccountMeta::new(accounts.sola_vault, false);

            self.accounts.market_vault = AccountMeta::new_readonly(accounts.market_vault, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_sola.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.sola_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: TransferAuthority
    // ....................................................................

    /// Main instruction struct for TransferAuthority
    pub struct TransferAuthorityInstruction {
        pub accounts: TransferAuthorityInstructionAccountMetas,
        pub data: TransferAuthorityInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for TransferAuthority instruction
    #[derive(Debug, Clone, Default)]
    pub struct TransferAuthorityInstructionAccountMetas {
        pub authority: AccountMeta,

        pub new_authority: AccountMeta,

        pub protocol_state: AccountMeta,
    }

    /// Account pubkeys for TransferAuthority instruction
    #[derive(Debug, Clone)]
    pub struct TransferAuthorityInstructionAccounts {
        pub authority: Pubkey,

        pub new_authority: Pubkey,

        pub protocol_state: Pubkey,
    }

    impl TransferAuthorityInstructionAccounts {
        pub fn new(authority: Pubkey, new_authority: Pubkey, protocol_state: Pubkey) -> Self {
            Self {
                authority,

                new_authority,

                protocol_state,
            }
        }
    }

    /// Instruction data for TransferAuthority
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct TransferAuthorityInstructionData {}

    impl TransferAuthorityInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for TransferAuthorityInstruction
    impl TransferAuthorityInstruction {
        fn discriminator() -> [u8; 8] {
            [48u8, 169u8, 76u8, 72u8, 229u8, 180u8, 55u8, 161u8]
        }

        pub fn data(data: TransferAuthorityInstructionData) -> Self {
            Self {
                accounts: TransferAuthorityInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: TransferAuthorityInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.new_authority = AccountMeta::new_readonly(accounts.new_authority, false);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.new_authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UnlockHiSola
    // ....................................................................

    /// Main instruction struct for UnlockHiSola
    pub struct UnlockHiSolaInstruction {
        pub accounts: UnlockHiSolaInstructionAccountMetas,
        pub data: UnlockHiSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UnlockHiSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnlockHiSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub lock_position: AccountMeta,

        pub ve_lock_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for UnlockHiSola instruction
    #[derive(Debug, Clone)]
    pub struct UnlockHiSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub lock_position: Pubkey,

        pub ve_lock_vault: Pubkey,

        pub market_vault: Pubkey,

        pub user_position: Pubkey,
    }

    impl UnlockHiSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            lock_position: Pubkey,

            ve_lock_vault: Pubkey,

            market_vault: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                hi_sola_mint,

                user_hi_sola,

                lock_position,

                ve_lock_vault,

                market_vault,

                user_position,
            }
        }
    }

    /// Instruction data for UnlockHiSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnlockHiSolaInstructionData {}

    impl UnlockHiSolaInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for UnlockHiSolaInstruction
    impl UnlockHiSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [123u8, 176u8, 206u8, 169u8, 48u8, 8u8, 182u8, 158u8]
        }

        pub fn data(data: UnlockHiSolaInstructionData) -> Self {
            Self {
                accounts: UnlockHiSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnlockHiSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new(accounts.user_hi_sola, false);

            self.accounts.lock_position = AccountMeta::new(accounts.lock_position, false);

            self.accounts.ve_lock_vault = AccountMeta::new(accounts.ve_lock_vault, false);

            self.accounts.market_vault = AccountMeta::new_readonly(accounts.market_vault, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.lock_position.clone());

            metas.push(self.accounts.ve_lock_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: Unpause
    // ....................................................................

    /// Main instruction struct for Unpause
    pub struct UnpauseInstruction {
        pub accounts: UnpauseInstructionAccountMetas,
        pub data: UnpauseInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for Unpause instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnpauseInstructionAccountMetas {
        pub authority: AccountMeta,

        pub protocol_state: AccountMeta,
    }

    /// Account pubkeys for Unpause instruction
    #[derive(Debug, Clone)]
    pub struct UnpauseInstructionAccounts {
        pub authority: Pubkey,

        pub protocol_state: Pubkey,
    }

    impl UnpauseInstructionAccounts {
        pub fn new(authority: Pubkey, protocol_state: Pubkey) -> Self {
            Self {
                authority,

                protocol_state,
            }
        }
    }

    /// Instruction data for Unpause
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnpauseInstructionData {}

    impl UnpauseInstructionData {
        pub fn new() -> Self {
            Self {}
        }
    }

    /// Implementation for UnpauseInstruction
    impl UnpauseInstruction {
        fn discriminator() -> [u8; 8] {
            [169u8, 144u8, 4u8, 38u8, 10u8, 141u8, 188u8, 255u8]
        }

        pub fn data(data: UnpauseInstructionData) -> Self {
            Self {
                accounts: UnpauseInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnpauseInstructionAccounts) -> Self {
            self.accounts.authority = AccountMeta::new_readonly(accounts.authority, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.authority.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: UnstakeHiSola
    // ....................................................................

    /// Main instruction struct for UnstakeHiSola
    pub struct UnstakeHiSolaInstruction {
        pub accounts: UnstakeHiSolaInstructionAccountMetas,
        pub data: UnstakeHiSolaInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for UnstakeHiSola instruction
    #[derive(Debug, Clone, Default)]
    pub struct UnstakeHiSolaInstructionAccountMetas {
        pub user: AccountMeta,

        pub protocol_state: AccountMeta,

        pub sola_mint: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub user_sola: AccountMeta,

        pub sola_vault: AccountMeta,

        pub market_vault: AccountMeta,

        pub usdc_mint: AccountMeta,

        pub user_usdc: AccountMeta,

        pub user_position: AccountMeta,

        pub token_program: AccountMeta,

        pub associated_token_program: AccountMeta,

        pub system_program: AccountMeta,
    }

    /// Account pubkeys for UnstakeHiSola instruction
    #[derive(Debug, Clone)]
    pub struct UnstakeHiSolaInstructionAccounts {
        pub user: Pubkey,

        pub protocol_state: Pubkey,

        pub sola_mint: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub user_sola: Pubkey,

        pub sola_vault: Pubkey,

        pub market_vault: Pubkey,

        pub usdc_mint: Pubkey,

        pub user_usdc: Pubkey,

        pub user_position: Pubkey,
    }

    impl UnstakeHiSolaInstructionAccounts {
        pub fn new(
            user: Pubkey,

            protocol_state: Pubkey,

            sola_mint: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            user_sola: Pubkey,

            sola_vault: Pubkey,

            market_vault: Pubkey,

            usdc_mint: Pubkey,

            user_usdc: Pubkey,

            user_position: Pubkey,
        ) -> Self {
            Self {
                user,

                protocol_state,

                sola_mint,

                hi_sola_mint,

                user_hi_sola,

                user_sola,

                sola_vault,

                market_vault,

                usdc_mint,

                user_usdc,

                user_position,
            }
        }
    }

    /// Instruction data for UnstakeHiSola
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct UnstakeHiSolaInstructionData {
        pub hi_sola_amount: u64,
    }

    impl UnstakeHiSolaInstructionData {
        pub fn new(hi_sola_amount: u64) -> Self {
            Self { hi_sola_amount }
        }
    }

    /// Implementation for UnstakeHiSolaInstruction
    impl UnstakeHiSolaInstruction {
        fn discriminator() -> [u8; 8] {
            [213u8, 39u8, 177u8, 36u8, 65u8, 52u8, 217u8, 25u8]
        }

        pub fn data(data: UnstakeHiSolaInstructionData) -> Self {
            Self {
                accounts: UnstakeHiSolaInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: UnstakeHiSolaInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.protocol_state = AccountMeta::new(accounts.protocol_state, false);

            self.accounts.sola_mint = AccountMeta::new(accounts.sola_mint, false);

            self.accounts.hi_sola_mint = AccountMeta::new(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new(accounts.user_hi_sola, false);

            self.accounts.user_sola = AccountMeta::new(accounts.user_sola, false);

            self.accounts.sola_vault = AccountMeta::new(accounts.sola_vault, false);

            self.accounts.market_vault = AccountMeta::new(accounts.market_vault, false);

            self.accounts.usdc_mint = AccountMeta::new_readonly(accounts.usdc_mint, false);

            self.accounts.user_usdc = AccountMeta::new(accounts.user_usdc, false);

            self.accounts.user_position = AccountMeta::new(accounts.user_position, false);

            self.accounts.token_program = AccountMeta::new_readonly(
                pubkey!("TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA"),
                false,
            );

            self.accounts.associated_token_program = AccountMeta::new_readonly(
                pubkey!("ATokenGPvbdGVxr1b2hvZbsiqW5xWH25efTNsLJA8knL"),
                false,
            );

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.sola_mint.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.user_sola.clone());

            metas.push(self.accounts.sola_vault.clone());

            metas.push(self.accounts.market_vault.clone());

            metas.push(self.accounts.usdc_mint.clone());

            metas.push(self.accounts.user_usdc.clone());

            metas.push(self.accounts.user_position.clone());

            metas.push(self.accounts.token_program.clone());

            metas.push(self.accounts.associated_token_program.clone());

            metas.push(self.accounts.system_program.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ....................................................................
    // Instruction: VoteGauge
    // ....................................................................

    /// Main instruction struct for VoteGauge
    pub struct VoteGaugeInstruction {
        pub accounts: VoteGaugeInstructionAccountMetas,
        pub data: VoteGaugeInstructionData,
        pub remaining_accounts: Vec<AccountMeta>,
    }

    /// Account metadata for VoteGauge instruction
    #[derive(Debug, Clone, Default)]
    pub struct VoteGaugeInstructionAccountMetas {
        pub user: AccountMeta,

        pub pool_id: AccountMeta,

        pub protocol_state: AccountMeta,

        pub hi_sola_mint: AccountMeta,

        pub user_hi_sola: AccountMeta,

        pub lock_position: AccountMeta,

        pub gauge_state: AccountMeta,

        pub user_vote_receipt: AccountMeta,

        pub user_epoch_votes: AccountMeta,

        pub global_epoch_votes: AccountMeta,

        pub system_program: AccountMeta,

        pub rent: AccountMeta,
    }

    /// Account pubkeys for VoteGauge instruction
    #[derive(Debug, Clone)]
    pub struct VoteGaugeInstructionAccounts {
        pub user: Pubkey,

        pub pool_id: Pubkey,

        pub protocol_state: Pubkey,

        pub hi_sola_mint: Pubkey,

        pub user_hi_sola: Pubkey,

        pub lock_position: Pubkey,

        pub gauge_state: Pubkey,

        pub user_vote_receipt: Pubkey,

        pub user_epoch_votes: Pubkey,

        pub global_epoch_votes: Pubkey,
    }

    impl VoteGaugeInstructionAccounts {
        pub fn new(
            user: Pubkey,

            pool_id: Pubkey,

            protocol_state: Pubkey,

            hi_sola_mint: Pubkey,

            user_hi_sola: Pubkey,

            lock_position: Pubkey,

            gauge_state: Pubkey,

            user_vote_receipt: Pubkey,

            user_epoch_votes: Pubkey,

            global_epoch_votes: Pubkey,
        ) -> Self {
            Self {
                user,

                pool_id,

                protocol_state,

                hi_sola_mint,

                user_hi_sola,

                lock_position,

                gauge_state,

                user_vote_receipt,

                user_epoch_votes,

                global_epoch_votes,
            }
        }
    }

    /// Instruction data for VoteGauge
    #[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
    pub struct VoteGaugeInstructionData {
        pub epoch: u64,

        pub votes: u64,
    }

    impl VoteGaugeInstructionData {
        pub fn new(epoch: u64, votes: u64) -> Self {
            Self { epoch, votes }
        }
    }

    /// Implementation for VoteGaugeInstruction
    impl VoteGaugeInstruction {
        fn discriminator() -> [u8; 8] {
            [181u8, 240u8, 7u8, 119u8, 123u8, 217u8, 116u8, 48u8]
        }

        pub fn data(data: VoteGaugeInstructionData) -> Self {
            Self {
                accounts: VoteGaugeInstructionAccountMetas::default(),
                data,
                remaining_accounts: Vec::new(),
            }
        }

        pub fn accounts(mut self, accounts: VoteGaugeInstructionAccounts) -> Self {
            self.accounts.user = AccountMeta::new(accounts.user, true);

            self.accounts.pool_id = AccountMeta::new_readonly(accounts.pool_id, false);

            self.accounts.protocol_state =
                AccountMeta::new_readonly(accounts.protocol_state, false);

            self.accounts.hi_sola_mint = AccountMeta::new_readonly(accounts.hi_sola_mint, false);

            self.accounts.user_hi_sola = AccountMeta::new_readonly(accounts.user_hi_sola, false);

            self.accounts.lock_position = AccountMeta::new_readonly(accounts.lock_position, false);

            self.accounts.gauge_state = AccountMeta::new(accounts.gauge_state, false);

            self.accounts.user_vote_receipt = AccountMeta::new(accounts.user_vote_receipt, false);

            self.accounts.user_epoch_votes = AccountMeta::new(accounts.user_epoch_votes, false);

            self.accounts.global_epoch_votes = AccountMeta::new(accounts.global_epoch_votes, false);

            self.accounts.system_program =
                AccountMeta::new_readonly(pubkey!("11111111111111111111111111111111"), false);

            self.accounts.rent = AccountMeta::new_readonly(
                pubkey!("SysvarRent111111111111111111111111111111111"),
                false,
            );

            self
        }

        pub fn remaining_accounts(mut self, accounts: Vec<AccountMeta>) -> Self {
            self.remaining_accounts = accounts;
            self
        }

        fn to_account_metas(&self) -> Vec<AccountMeta> {
            let mut metas = Vec::new();

            metas.push(self.accounts.user.clone());

            metas.push(self.accounts.pool_id.clone());

            metas.push(self.accounts.protocol_state.clone());

            metas.push(self.accounts.hi_sola_mint.clone());

            metas.push(self.accounts.user_hi_sola.clone());

            metas.push(self.accounts.lock_position.clone());

            metas.push(self.accounts.gauge_state.clone());

            metas.push(self.accounts.user_vote_receipt.clone());

            metas.push(self.accounts.user_epoch_votes.clone());

            metas.push(self.accounts.global_epoch_votes.clone());

            metas.push(self.accounts.system_program.clone());

            metas.push(self.accounts.rent.clone());

            metas.extend(self.remaining_accounts.clone());
            metas
        }

        pub fn instruction(&self) -> Instruction {
            let mut buffer: Vec<u8> = Vec::new();

            buffer.extend_from_slice(&Self::discriminator());

            self.data.serialize(&mut buffer).unwrap();

            Instruction::new_with_bytes(program_id(), &buffer, self.to_account_metas())
        }
    }

    // ------------------------------------------------------------------------
    // Composite Accounts
    // ------------------------------------------------------------------------
}

// ============================================================================
// CUSTOM TYPES
// ============================================================================

/// Custom struct: AmmPool
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct AmmPool {
    pub token_a_mint: Pubkey,

    pub token_b_mint: Pubkey,

    pub token_a_vault: Pubkey,

    pub token_b_vault: Pubkey,

    pub lp_mint: Pubkey,

    pub fee_rate: u16,

    pub protocol_fee_bps: u16,

    pub total_lp: u64,

    pub reserve_a: u64,

    pub reserve_b: u64,

    pub bump: u8,

    pub osola_reward_per_lp: u128,

    pub last_reward_ts: i64,
}

impl AmmPool {
    pub fn new(
        token_a_mint: Pubkey,

        token_b_mint: Pubkey,

        token_a_vault: Pubkey,

        token_b_vault: Pubkey,

        lp_mint: Pubkey,

        fee_rate: u16,

        protocol_fee_bps: u16,

        total_lp: u64,

        reserve_a: u64,

        reserve_b: u64,

        bump: u8,

        osola_reward_per_lp: u128,

        last_reward_ts: i64,
    ) -> Self {
        Self {
            token_a_mint,

            token_b_mint,

            token_a_vault,

            token_b_vault,

            lp_mint,

            fee_rate,

            protocol_fee_bps,

            total_lp,

            reserve_a,

            reserve_b,

            bump,

            osola_reward_per_lp,

            last_reward_ts,
        }
    }
}

/// Custom struct: BribeVault
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct BribeVault {
    pub pool_id: Pubkey,

    pub reward_mint: Pubkey,

    pub epoch: u64,

    pub total_bribed: u64,

    pub bump: u8,
}

impl BribeVault {
    pub fn new(
        pool_id: Pubkey,

        reward_mint: Pubkey,

        epoch: u64,

        total_bribed: u64,

        bump: u8,
    ) -> Self {
        Self {
            pool_id,

            reward_mint,

            epoch,

            total_bribed,

            bump,
        }
    }
}

/// Custom struct: ContributorVesting
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ContributorVesting {
    pub contributor: Pubkey,

    pub hi_sola_amount: u64,

    pub o_sola_amount: u64,

    pub hi_sola_claimed: u64,

    pub o_sola_claimed: u64,

    pub start_ts: i64,

    pub bump: u8,
}

impl ContributorVesting {
    pub fn new(
        contributor: Pubkey,

        hi_sola_amount: u64,

        o_sola_amount: u64,

        hi_sola_claimed: u64,

        o_sola_claimed: u64,

        start_ts: i64,

        bump: u8,
    ) -> Self {
        Self {
            contributor,

            hi_sola_amount,

            o_sola_amount,

            hi_sola_claimed,

            o_sola_claimed,

            start_ts,

            bump,
        }
    }
}

/// Custom struct: FounderHiSolaVesting
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct FounderHiSolaVesting {
    pub total_amount: u64,

    pub claimed: u64,

    pub start_ts: i64,

    pub bump: u8,
}

impl FounderHiSolaVesting {
    pub fn new(total_amount: u64, claimed: u64, start_ts: i64, bump: u8) -> Self {
        Self {
            total_amount,

            claimed,

            start_ts,

            bump,
        }
    }
}

/// Custom struct: FounderVesting
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct FounderVesting {
    pub total_amount: u64,

    pub claimed: u64,

    pub start_ts: i64,

    pub bump: u8,
}

impl FounderVesting {
    pub fn new(total_amount: u64, claimed: u64, start_ts: i64, bump: u8) -> Self {
        Self {
            total_amount,

            claimed,

            start_ts,

            bump,
        }
    }
}

/// Custom struct: GaugeState
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GaugeState {
    pub pool_id: Pubkey,

    pub epoch: u64,

    pub total_votes: u64,

    pub bump: u8,
}

impl GaugeState {
    pub fn new(pool_id: Pubkey, epoch: u64, total_votes: u64, bump: u8) -> Self {
        Self {
            pool_id,

            epoch,

            total_votes,

            bump,
        }
    }
}

/// Custom struct: GlobalEpochVotes
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct GlobalEpochVotes {
    pub epoch: u64,

    pub total_votes: u64,

    pub bump: u8,
}

impl GlobalEpochVotes {
    pub fn new(epoch: u64, total_votes: u64, bump: u8) -> Self {
        Self {
            epoch,

            total_votes,

            bump,
        }
    }
}

/// Custom struct: LpEpochClaim
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct LpEpochClaim {
    pub bump: u8,
}

impl LpEpochClaim {
    pub fn new(bump: u8) -> Self {
        Self { bump }
    }
}

/// Custom struct: LpPoolEpochAccum
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct LpPoolEpochAccum {
    pub pool: Pubkey,

    pub epoch: u64,

    pub total_weighted_supply: u128,

    pub last_update_ts: i64,

    pub last_lp_supply: u64,

    pub osola_allocated: u64,

    pub finalized: bool,

    pub bump: u8,
}

impl LpPoolEpochAccum {
    pub fn new(
        pool: Pubkey,

        epoch: u64,

        total_weighted_supply: u128,

        last_update_ts: i64,

        last_lp_supply: u64,

        osola_allocated: u64,

        finalized: bool,

        bump: u8,
    ) -> Self {
        Self {
            pool,

            epoch,

            total_weighted_supply,

            last_update_ts,

            last_lp_supply,

            osola_allocated,

            finalized,

            bump,
        }
    }
}

/// Custom struct: LpUserCheckpoint
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct LpUserCheckpoint {
    pub user: Pubkey,

    pub pool: Pubkey,

    pub weighted_balance: u128,

    pub last_update_ts: i64,

    pub last_epoch: u64,

    pub bump: u8,
}

impl LpUserCheckpoint {
    pub fn new(
        user: Pubkey,

        pool: Pubkey,

        weighted_balance: u128,

        last_update_ts: i64,

        last_epoch: u64,

        bump: u8,
    ) -> Self {
        Self {
            user,

            pool,

            weighted_balance,

            last_update_ts,

            last_epoch,

            bump,
        }
    }
}

/// Custom struct: LpUserInfo
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct LpUserInfo {
    pub reward_debt: u128,

    pub bump: u8,
}

impl LpUserInfo {
    pub fn new(reward_debt: u128, bump: u8) -> Self {
        Self { reward_debt, bump }
    }
}

/// Custom struct: PolState
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct PolState {
    pub pol_split_bps: u16,

    pub target_pool: Pubkey,

    pub usdc_accumulated: u64,

    pub bump: u8,
}

impl PolState {
    pub fn new(pol_split_bps: u16, target_pool: Pubkey, usdc_accumulated: u64, bump: u8) -> Self {
        Self {
            pol_split_bps,

            target_pool,

            usdc_accumulated,

            bump,
        }
    }
}

/// Custom struct: ProtocolState
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct ProtocolState {
    pub authority: Pubkey,

    pub usdc_mint: Pubkey,

    pub sola_mint: Pubkey,

    pub hi_sola_mint: Pubkey,

    pub o_sola_mint: Pubkey,

    pub floor_vault: Pubkey,

    pub market_vault: Pubkey,

    pub sola_vault: Pubkey,

    pub virtual_usdc: u64,

    pub virtual_sola: u64,

    pub k: u128,

    pub total_sola: u64,

    pub total_hi_sola: u64,

    pub accumulated_fees: u64,

    pub fees_per_hi_sola: u128,

    pub last_market_vault_balance: u64,

    pub bump: u8,

    pub founder_allocated: bool,

    pub ecosystem_allocated: bool,

    pub total_usdc_borrowed: u64,

    pub total_purchased_sola: u64,

    pub paused: bool,
}

impl ProtocolState {
    pub fn new(
        authority: Pubkey,

        usdc_mint: Pubkey,

        sola_mint: Pubkey,

        hi_sola_mint: Pubkey,

        o_sola_mint: Pubkey,

        floor_vault: Pubkey,

        market_vault: Pubkey,

        sola_vault: Pubkey,

        virtual_usdc: u64,

        virtual_sola: u64,

        k: u128,

        total_sola: u64,

        total_hi_sola: u64,

        accumulated_fees: u64,

        fees_per_hi_sola: u128,

        last_market_vault_balance: u64,

        bump: u8,

        founder_allocated: bool,

        ecosystem_allocated: bool,

        total_usdc_borrowed: u64,

        total_purchased_sola: u64,

        paused: bool,
    ) -> Self {
        Self {
            authority,

            usdc_mint,

            sola_mint,

            hi_sola_mint,

            o_sola_mint,

            floor_vault,

            market_vault,

            sola_vault,

            virtual_usdc,

            virtual_sola,

            k,

            total_sola,

            total_hi_sola,

            accumulated_fees,

            fees_per_hi_sola,

            last_market_vault_balance,

            bump,

            founder_allocated,

            ecosystem_allocated,

            total_usdc_borrowed,

            total_purchased_sola,

            paused,
        }
    }
}

/// Custom struct: UserBribeClaim
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UserBribeClaim {
    pub bump: u8,
}

impl UserBribeClaim {
    pub fn new(bump: u8) -> Self {
        Self { bump }
    }
}

/// Custom struct: UserEpochVotes
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UserEpochVotes {
    pub epoch: u64,

    pub allocated: u64,

    pub total_power_snapshot: u64,

    pub bump: u8,
}

impl UserEpochVotes {
    pub fn new(epoch: u64, allocated: u64, total_power_snapshot: u64, bump: u8) -> Self {
        Self {
            epoch,

            allocated,

            total_power_snapshot,

            bump,
        }
    }
}

/// Custom struct: UserPosition
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UserPosition {
    pub owner: Pubkey,

    pub usdc_borrowed: u64,

    pub fees_debt: u128,

    pub bump: u8,

    pub last_borrow_slot: u64,
}

impl UserPosition {
    pub fn new(
        owner: Pubkey,

        usdc_borrowed: u64,

        fees_debt: u128,

        bump: u8,

        last_borrow_slot: u64,
    ) -> Self {
        Self {
            owner,

            usdc_borrowed,

            fees_debt,

            bump,

            last_borrow_slot,
        }
    }
}

/// Custom struct: UserVoteReceipt
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct UserVoteReceipt {
    pub user: Pubkey,

    pub pool_id: Pubkey,

    pub epoch: u64,

    pub votes: u64,

    pub bump: u8,
}

impl UserVoteReceipt {
    pub fn new(user: Pubkey, pool_id: Pubkey, epoch: u64, votes: u64, bump: u8) -> Self {
        Self {
            user,

            pool_id,

            epoch,

            votes,

            bump,
        }
    }
}

/// Custom struct: VeLockPosition
#[derive(Debug, BorshDeserialize, BorshSerialize, Clone)]
pub struct VeLockPosition {
    pub owner: Pubkey,

    pub amount_locked: u64,

    pub lock_end_ts: i64,

    pub bump: u8,
}

impl VeLockPosition {
    pub fn new(owner: Pubkey, amount_locked: u64, lock_end_ts: i64, bump: u8) -> Self {
        Self {
            owner,

            amount_locked,

            lock_end_ts,

            bump,
        }
    }
}

// ============================================================================
// END OF GENERATED FILE
// ============================================================================
